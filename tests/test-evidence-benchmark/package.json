{
  "private": true,
  "name": "test-evidence-benchmark",
  "version": "0.28.6",
  "description": "End-to-end feature tests for the benchmark template, standing the real workspace up and running its own gates.",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "start": "node ../../packages/ttsc/lib/launcher/ttsx.js --project tsconfig.json src/index.ts"
  },
  "devDependencies": {
    "@nestia/e2e": "catalog:utils",
    "@types/node": "catalog:utils",
    "ttsc": "workspace:*",
    "typescript": "catalog:typescript",
    "typia": "catalog:samchon",
    "yaml": "catalog:utils"
  }
}
