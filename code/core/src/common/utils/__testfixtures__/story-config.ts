/** Fixture: the shape a story file's `meta` is composed from, in a module of its own. */
import { ButtonComponent } from './button.component';

export const config = { component: ButtonComponent, args: {} };
