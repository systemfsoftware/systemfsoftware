// The public definePreview lives in the framework package; the harness only depends on the renderer.
import { __definePreview } from '@storybook/vue3';

export default __definePreview({ addons: [] });
