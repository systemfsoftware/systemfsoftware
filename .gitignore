node_modules/
dist/
.turbo/
coverage/
reports/
# Test fixtures ship a prior mutation report as tracked input data
!**/test/**/fixtures/**/reports/
.stryker-tmp/
temp/
*.tsbuildinfo
.DS_Store
__pycache__/
.npmrc
# Agent scratch — ce-optimize sweeps, audit reports, run logs
.context/
# Vendored subtrees — their .gitignore files are inert
repos/**/.gitignore
repos/**/.codegraph/
.repos
.issues
wiki
research
**/__pycache__/
- Local per-clone hooks (machine-specific: qmd-mcp orphan cleanup, etc.)
.claude/settings.local.json
