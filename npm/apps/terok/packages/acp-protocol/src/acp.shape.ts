// GENERATED by scripts/generate-schema.ts — do not edit.
// Source of truth: protocol/acp-schema.json (262 definitions).
// Regenerate with: pnpm --filter @terok/acp-protocol codegen
//
// Constraints the JSON Schema states that Effect Schema cannot express, so the
// generated union admits shapes the wire format rejects. A decoder needing the
// exclusion applies its own filter:
//   $defs.MultiSelectItems
//     JSON Schema `not`: mutual exclusion between object shapes; emitted union is permissive
//   $defs.ElicitationPropertySchema
//     JSON Schema `not`: mutual exclusion between object shapes; emitted union is permissive
//   $defs.CreateElicitationRequest
//     JSON Schema `not`: mutual exclusion between object shapes; emitted union is permissive
//   $defs.CreateElicitationResponse
//     JSON Schema `not`: mutual exclusion between object shapes; emitted union is permissive

import * as S from 'effect/Schema'

/**
 * An arbitrary JSON value, as carried by the protocol's open fields.
 *
 * `_meta` (195 of the 197 open objects) is ACP's extensibility slot: its
 * contents are not specified, but they did arrive as JSON, so `unknown` is
 * wider than the truth — it admits NaN, `undefined` and functions that no
 * wire value can be.
 *
 * `__proto__` is refused rather than carried. Effect's `S.Record` decode
 * assigns keys instead of defining them, so an own `__proto__` key — which
 * `JSON.parse` produces safely — becomes a real prototype assignment, and the
 * decoded object silently inherits attacker-controlled properties (measured on
 * effect 3.22.1). No legitimate peer sends it as a metadata key.
 */
export const JsonObjectKey = S.String.pipe(
  S.filter((k) => k !== '__proto__', {
    identifier: 'JsonObjectKey',
    description: 'a JSON object key that cannot reach Object.prototype',
  }),
)
export type JsonObjectKey = typeof JsonObjectKey.Type

export type JsonValue =
  | null
  | boolean
  | number
  | string
  | ReadonlyArray<JsonValue>
  | { readonly [key: string]: JsonValue }

export const JsonValue: S.Schema<JsonValue> = S.suspend(() =>
  S.Union(
    S.Null,
    S.Boolean,
    S.JsonNumber,
    S.String,
    S.Array(JsonValue),
    S.Record({ key: JsonObjectKey, value: JsonValue }),
  )
)

export const JsonObject = S.Record({ key: JsonObjectKey, value: JsonValue })
export type JsonObject = typeof JsonObject.Type

/**
 * JSON RPC Request Id
 *
 * An identifier established by the Client that MUST contain a String, Number, or NULL value if included. If it is not included it is assumed to be a notification. The value SHOULD normally not be Null \[1\] and Numbers SHOULD NOT contain fractional parts \[2\]
 *
 * The Server MUST reply with the same value in the Response object if included. This member is used to correlate the context between the two objects.
 *
 * \[1\] The use of Null as a value for the id member in a Request object is discouraged, because this specification uses a value of Null for Responses with an unknown id. Also, because JSON-RPC 1.0 uses an id value of Null for Notifications this could cause confusion in handling.
 *
 * \[2\] Fractional parts may be problematic, since many decimal fractions cannot be represented exactly as binary fractions.
 */
export const RequestId = S.NullOr(S.Union(S.Int, S.String))
export type RequestId = typeof RequestId.Type

/**
 * A unique identifier for a conversation session between a client and agent.
 *
 * Sessions maintain their own context, conversation history, and state,
 * allowing multiple independent interactions with the same agent.
 *
 * See protocol docs: [Session ID](https://agentclientprotocol.com/protocol/session-setup#session-id)
 */
export const SessionId = S.String
export type SessionId = typeof SessionId.Type

/**
 * Request to write content to a text file.
 *
 * Only available if the client supports the `fs.writeTextFile` capability.
 */
export const WriteTextFileRequest = S.Struct({
  sessionId: SessionId,
  path: S.String,
  content: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type WriteTextFileRequest = typeof WriteTextFileRequest.Type

/**
 * Request to read content from a text file.
 *
 * Only available if the client supports the `fs.readTextFile` capability.
 */
export const ReadTextFileRequest = S.Struct({
  sessionId: SessionId,
  path: S.String,
  line: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  limit: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ReadTextFileRequest = typeof ReadTextFileRequest.Type

/**
 * Unique identifier for a tool call within a session.
 */
export const ToolCallId = S.String
export type ToolCallId = typeof ToolCallId.Type

/**
 * Categories of tools that can be invoked.
 *
 * Tool kinds help clients choose appropriate icons and optimize how they
 * display tool execution progress.
 *
 * See protocol docs: [Creating](https://agentclientprotocol.com/protocol/tool-calls#creating)
 */
export const ToolKind = S.Literal(
  'read',
  'edit',
  'delete',
  'move',
  'search',
  'execute',
  'think',
  'fetch',
  'switch_mode',
  'other',
)
export type ToolKind = typeof ToolKind.Type

/**
 * Execution status of a tool call.
 *
 * Tool calls progress through different statuses during their lifecycle.
 *
 * See protocol docs: [Status](https://agentclientprotocol.com/protocol/tool-calls#status)
 */
export const ToolCallStatus = S.Literal('pending', 'in_progress', 'completed', 'failed')
export type ToolCallStatus = typeof ToolCallStatus.Type

/**
 * The sender or recipient of messages and data in a conversation.
 */
export const Role = S.Literal('assistant', 'user')
export type Role = typeof Role.Type

/**
 * Optional annotations for the client. The client can use annotations to inform how objects are used or displayed
 */
export const Annotations = S.Struct({
  audience: S.optional(S.NullOr(S.Array(Role))),
  lastModified: S.optional(S.NullOr(S.String)),
  priority: S.optional(S.NullOr(S.JsonNumber)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type Annotations = typeof Annotations.Type

/**
 * Text provided to or from an LLM.
 */
export const TextContent = S.Struct({
  annotations: S.optional(S.NullOr(Annotations)),
  text: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type TextContent = typeof TextContent.Type

/**
 * An image provided to or from an LLM.
 */
export const ImageContent = S.Struct({
  annotations: S.optional(S.NullOr(Annotations)),
  data: S.String,
  mimeType: S.String,
  uri: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ImageContent = typeof ImageContent.Type

/**
 * Audio provided to or from an LLM.
 */
export const AudioContent = S.Struct({
  annotations: S.optional(S.NullOr(Annotations)),
  data: S.String,
  mimeType: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AudioContent = typeof AudioContent.Type

/**
 * A resource that the server is capable of reading, included in a prompt or tool call result.
 */
export const ResourceLink = S.Struct({
  annotations: S.optional(S.NullOr(Annotations)),
  description: S.optional(S.NullOr(S.String)),
  mimeType: S.optional(S.NullOr(S.String)),
  name: S.String,
  size: S.optional(S.NullOr(S.Int)),
  title: S.optional(S.NullOr(S.String)),
  uri: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ResourceLink = typeof ResourceLink.Type

/**
 * Text-based resource contents.
 */
export const TextResourceContents = S.Struct({
  mimeType: S.optional(S.NullOr(S.String)),
  text: S.String,
  uri: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type TextResourceContents = typeof TextResourceContents.Type

/**
 * Binary resource contents.
 */
export const BlobResourceContents = S.Struct({
  blob: S.String,
  mimeType: S.optional(S.NullOr(S.String)),
  uri: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type BlobResourceContents = typeof BlobResourceContents.Type

/**
 * Resource content that can be embedded in a message.
 */
export const EmbeddedResourceResource = S.Union(TextResourceContents, BlobResourceContents)
export type EmbeddedResourceResource = typeof EmbeddedResourceResource.Type

/**
 * The contents of a resource, embedded into a prompt or tool call result.
 */
export const EmbeddedResource = S.Struct({
  annotations: S.optional(S.NullOr(Annotations)),
  resource: EmbeddedResourceResource,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type EmbeddedResource = typeof EmbeddedResource.Type

/**
 * Content blocks represent displayable information in the Agent Client Protocol.
 *
 * They provide a structured way to handle various types of user-facing content—whether
 * it's text from language models, images for analysis, or embedded resources for context.
 *
 * Content blocks appear in:
 * - User prompts sent via `session/prompt`
 * - Language model output streamed through `session/update` notifications
 * - Progress updates and results from tool calls
 *
 * This structure is compatible with the Model Context Protocol (MCP), enabling
 * agents to seamlessly forward content from MCP tool outputs without transformation.
 *
 * See protocol docs: [Content](https://agentclientprotocol.com/protocol/content)
 */
export const ContentBlock = S.Union(
  S.extend(
    TextContent,
    S.Struct({
      type: S.Literal('text'),
    }),
  ),
  S.extend(
    ImageContent,
    S.Struct({
      type: S.Literal('image'),
    }),
  ),
  S.extend(
    AudioContent,
    S.Struct({
      type: S.Literal('audio'),
    }),
  ),
  S.extend(
    ResourceLink,
    S.Struct({
      type: S.Literal('resource_link'),
    }),
  ),
  S.extend(
    EmbeddedResource,
    S.Struct({
      type: S.Literal('resource'),
    }),
  ),
)
export type ContentBlock = typeof ContentBlock.Type

/**
 * Standard content block (text, images, resources).
 */
export const Content = S.Struct({
  content: ContentBlock,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type Content = typeof Content.Type

/**
 * A diff representing file modifications.
 *
 * Shows changes to files in a format suitable for display in the client UI.
 *
 * See protocol docs: [Content](https://agentclientprotocol.com/protocol/tool-calls#content)
 */
export const Diff = S.Struct({
  path: S.String,
  oldText: S.optional(S.NullOr(S.String)),
  newText: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type Diff = typeof Diff.Type

/**
 * Typed identifier used for terminal values on the wire.
 */
export const TerminalId = S.String
export type TerminalId = typeof TerminalId.Type

/**
 * Embed a terminal created with `terminal/create` by its id.
 *
 * The terminal must be added before calling `terminal/release`.
 *
 * See protocol docs: [Terminal](https://agentclientprotocol.com/protocol/terminals)
 */
export const Terminal = S.Struct({
  terminalId: TerminalId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type Terminal = typeof Terminal.Type

/**
 * Content produced by a tool call.
 *
 * Tool calls can produce different types of content including
 * standard content blocks (text, images) or file diffs.
 *
 * See protocol docs: [Content](https://agentclientprotocol.com/protocol/tool-calls#content)
 */
export const ToolCallContent = S.Union(
  S.extend(
    Content,
    S.Struct({
      type: S.Literal('content'),
    }),
  ),
  S.extend(
    Diff,
    S.Struct({
      type: S.Literal('diff'),
    }),
  ),
  S.extend(
    Terminal,
    S.Struct({
      type: S.Literal('terminal'),
    }),
  ),
)
export type ToolCallContent = typeof ToolCallContent.Type

/**
 * A file location being accessed or modified by a tool.
 *
 * Enables clients to implement "follow-along" features that track
 * which files the agent is working with in real-time.
 *
 * See protocol docs: [Following the Agent](https://agentclientprotocol.com/protocol/tool-calls#following-the-agent)
 */
export const ToolCallLocation = S.Struct({
  path: S.String,
  line: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ToolCallLocation = typeof ToolCallLocation.Type

/**
 * An update to an existing tool call.
 *
 * Used to report progress and results as tools execute. All fields except
 * the tool call ID are optional - only changed fields need to be included.
 *
 * See protocol docs: [Updating](https://agentclientprotocol.com/protocol/tool-calls#updating)
 */
export const ToolCallUpdate = S.Struct({
  toolCallId: ToolCallId,
  kind: S.optional(S.NullOr(ToolKind)),
  status: S.optional(S.NullOr(ToolCallStatus)),
  title: S.optional(S.NullOr(S.String)),
  name: S.optional(S.NullOr(S.String)),
  content: S.optional(S.NullOr(S.Array(ToolCallContent))),
  locations: S.optional(S.NullOr(S.Array(ToolCallLocation))),
  rawInput: S.optional(JsonValue),
  rawOutput: S.optional(JsonValue),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ToolCallUpdate = typeof ToolCallUpdate.Type

/**
 * Unique identifier for a permission option.
 */
export const PermissionOptionId = S.String
export type PermissionOptionId = typeof PermissionOptionId.Type

/**
 * The type of permission option being presented to the user.
 *
 * Helps clients choose appropriate icons and UI treatment.
 */
export const PermissionOptionKind = S.Literal('allow_once', 'allow_always', 'reject_once', 'reject_always')
export type PermissionOptionKind = typeof PermissionOptionKind.Type

/**
 * An option presented to the user when requesting permission.
 */
export const PermissionOption = S.Struct({
  optionId: PermissionOptionId,
  name: S.String,
  kind: PermissionOptionKind,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PermissionOption = typeof PermissionOption.Type

/**
 * Request for user permission to execute a tool call.
 *
 * Sent when the agent needs authorization before performing a sensitive operation.
 *
 * See protocol docs: [Requesting Permission](https://agentclientprotocol.com/protocol/tool-calls#requesting-permission)
 */
export const RequestPermissionRequest = S.Struct({
  sessionId: SessionId,
  toolCall: ToolCallUpdate,
  options: S.Array(PermissionOption),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type RequestPermissionRequest = typeof RequestPermissionRequest.Type

/**
 * An environment variable to set when launching an MCP server.
 */
export const EnvVariable = S.Struct({
  name: S.String,
  value: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type EnvVariable = typeof EnvVariable.Type

/**
 * Request to create a new terminal and execute a command.
 */
export const CreateTerminalRequest = S.Struct({
  sessionId: SessionId,
  command: S.String,
  args: S.optional(S.Array(S.String)),
  env: S.optional(S.Array(EnvVariable)),
  cwd: S.optional(S.NullOr(S.String)),
  outputByteLimit: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type CreateTerminalRequest = typeof CreateTerminalRequest.Type

/**
 * Request to get the current output and status of a terminal.
 */
export const TerminalOutputRequest = S.Struct({
  sessionId: SessionId,
  terminalId: TerminalId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type TerminalOutputRequest = typeof TerminalOutputRequest.Type

/**
 * Request to release a terminal and free its resources.
 */
export const ReleaseTerminalRequest = S.Struct({
  sessionId: SessionId,
  terminalId: TerminalId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ReleaseTerminalRequest = typeof ReleaseTerminalRequest.Type

/**
 * Request to wait for a terminal command to exit.
 */
export const WaitForTerminalExitRequest = S.Struct({
  sessionId: SessionId,
  terminalId: TerminalId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type WaitForTerminalExitRequest = typeof WaitForTerminalExitRequest.Type

/**
 * Request to kill a terminal without releasing it.
 */
export const KillTerminalRequest = S.Struct({
  sessionId: SessionId,
  terminalId: TerminalId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type KillTerminalRequest = typeof KillTerminalRequest.Type

/**
 * Type discriminator for elicitation schemas.
 */
export const ElicitationSchemaType = S.Literal('object')
export type ElicitationSchemaType = typeof ElicitationSchemaType.Type

/**
 * String format types for string properties in elicitation schemas.
 */
export const StringFormat = S.Literal('email', 'uri', 'date', 'date-time')
export type StringFormat = typeof StringFormat.Type

/**
 * A titled enum option with a const value, human-readable title, and optional description.
 */
export const EnumOption = S.Struct({
  const: S.String,
  title: S.String,
  description: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type EnumOption = typeof EnumOption.Type

/**
 * Schema for string properties in an elicitation form.
 *
 * When `enum` or `oneOf` is set, this represents a single-select enum
 * with `"type": "string"`.
 */
export const StringPropertySchema = S.Struct({
  title: S.optional(S.NullOr(S.String)),
  description: S.optional(S.NullOr(S.String)),
  minLength: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  maxLength: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  pattern: S.optional(S.NullOr(S.String)),
  format: S.optional(S.NullOr(StringFormat)),
  default: S.optional(S.NullOr(S.String)),
  enum: S.optional(S.NullOr(S.Array(S.String))),
  oneOf: S.optional(S.NullOr(S.Array(EnumOption))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type StringPropertySchema = typeof StringPropertySchema.Type

/**
 * Schema for number (floating-point) properties in an elicitation form.
 */
export const NumberPropertySchema = S.Struct({
  title: S.optional(S.NullOr(S.String)),
  description: S.optional(S.NullOr(S.String)),
  minimum: S.optional(S.NullOr(S.JsonNumber)),
  maximum: S.optional(S.NullOr(S.JsonNumber)),
  default: S.optional(S.NullOr(S.JsonNumber)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NumberPropertySchema = typeof NumberPropertySchema.Type

/**
 * Schema for integer properties in an elicitation form.
 */
export const IntegerPropertySchema = S.Struct({
  title: S.optional(S.NullOr(S.String)),
  description: S.optional(S.NullOr(S.String)),
  minimum: S.optional(S.NullOr(S.Int)),
  maximum: S.optional(S.NullOr(S.Int)),
  default: S.optional(S.NullOr(S.Int)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type IntegerPropertySchema = typeof IntegerPropertySchema.Type

/**
 * Schema for boolean properties in an elicitation form.
 */
export const BooleanPropertySchema = S.Struct({
  title: S.optional(S.NullOr(S.String)),
  description: S.optional(S.NullOr(S.String)),
  default: S.optional(S.NullOr(S.Boolean)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type BooleanPropertySchema = typeof BooleanPropertySchema.Type

/**
 * String item schema for multi-select enum properties.
 */
export const StringMultiSelectItems = S.Struct({
  enum: S.Array(S.String),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type StringMultiSelectItems = typeof StringMultiSelectItems.Type

/**
 * Items definition for titled multi-select enum properties.
 */
export const TitledMultiSelectItems = S.Struct({
  anyOf: S.Array(EnumOption),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type TitledMultiSelectItems = typeof TitledMultiSelectItems.Type

/**
 * Items for a multi-select (array) property schema.
 */
export const MultiSelectItems = S.Union(
  S.extend(
    StringMultiSelectItems,
    S.Struct({
      type: S.Literal('string'),
    }),
  ),
  TitledMultiSelectItems,
)
export type MultiSelectItems = typeof MultiSelectItems.Type

/**
 * Schema for multi-select (array) properties in an elicitation form.
 */
export const MultiSelectPropertySchema = S.Struct({
  title: S.optional(S.NullOr(S.String)),
  description: S.optional(S.NullOr(S.String)),
  minItems: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  maxItems: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  items: MultiSelectItems,
  default: S.optional(S.NullOr(S.Array(S.String))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type MultiSelectPropertySchema = typeof MultiSelectPropertySchema.Type

/**
 * Property schema for elicitation form fields.
 *
 * Each variant corresponds to a JSON Schema `"type"` value.
 * Single-select enums use the `String` variant with `enum` or `oneOf` set.
 * Multi-select enums use the `Array` variant.
 */
export const ElicitationPropertySchema = S.Union(
  S.extend(
    StringPropertySchema,
    S.Struct({
      type: S.Literal('string'),
    }),
  ),
  S.extend(
    NumberPropertySchema,
    S.Struct({
      type: S.Literal('number'),
    }),
  ),
  S.extend(
    IntegerPropertySchema,
    S.Struct({
      type: S.Literal('integer'),
    }),
  ),
  S.extend(
    BooleanPropertySchema,
    S.Struct({
      type: S.Literal('boolean'),
    }),
  ),
  S.extend(
    MultiSelectPropertySchema,
    S.Struct({
      type: S.Literal('array'),
    }),
  ),
)
export type ElicitationPropertySchema = typeof ElicitationPropertySchema.Type

/**
 * Type-safe elicitation schema for requesting structured user input.
 *
 * This represents a JSON Schema object with primitive-typed properties,
 * as required by the elicitation specification.
 */
export const ElicitationSchema = S.Struct({
  type: S.optional(ElicitationSchemaType),
  title: S.optional(S.NullOr(S.String)),
  properties: S.optional(S.Record({ key: JsonObjectKey, value: ElicitationPropertySchema })),
  required: S.optional(S.NullOr(S.Array(S.String))),
  description: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ElicitationSchema = typeof ElicitationSchema.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Session-scoped elicitation, optionally tied to a specific tool call.
 *
 * When `tool_call_id` is set, the elicitation is tied to a specific tool call.
 * This is useful when an agent receives an elicitation from an MCP server
 * during a tool call and needs to redirect it to the user.
 */
export const ElicitationSessionScope = S.Struct({
  sessionId: SessionId,
  toolCallId: S.optional(S.NullOr(ToolCallId)),
})
export type ElicitationSessionScope = typeof ElicitationSessionScope.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Request-scoped elicitation, tied to a specific JSON-RPC request outside of a session
 * (e.g., during auth/configuration phases before any session is started).
 */
export const ElicitationRequestScope = S.Struct({
  requestId: RequestId,
})
export type ElicitationRequestScope = typeof ElicitationRequestScope.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Form-based elicitation mode where the client renders a form from the provided schema.
 */
export const ElicitationFormMode = S.Union(ElicitationSessionScope, ElicitationRequestScope)
export type ElicitationFormMode = typeof ElicitationFormMode.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Unique identifier for an elicitation.
 */
export const ElicitationId = S.String
export type ElicitationId = typeof ElicitationId.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * URL-based elicitation mode where the client directs the user to a URL.
 */
export const ElicitationUrlMode = S.Union(ElicitationSessionScope, ElicitationRequestScope)
export type ElicitationUrlMode = typeof ElicitationUrlMode.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Request from the agent to elicit structured user input.
 *
 * The agent sends this to the client to request information from the user,
 * either via a form or by directing them to a URL.
 * Elicitations are tied to a session (optionally a tool call) or a request.
 */
export const CreateElicitationRequest = S.Union(
  S.extend(
    ElicitationFormMode,
    S.Struct({
      mode: S.Literal('form'),
    }),
  ),
  S.extend(
    ElicitationUrlMode,
    S.Struct({
      mode: S.Literal('url'),
    }),
  ),
)
export type CreateElicitationRequest = typeof CreateElicitationRequest.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Unique identifier for an MCP server using the ACP transport.
 *
 * The value is opaque and generated by the ACP component providing the MCP server. It is
 * used by `mcp/connect` to route connection requests back to the component that declared the
 * server.
 */
export const McpServerAcpId = S.String
export type McpServerAcpId = typeof McpServerAcpId.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Request parameters for `mcp/connect`.
 */
export const ConnectMcpRequest = S.Struct({
  serverId: McpServerAcpId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ConnectMcpRequest = typeof ConnectMcpRequest.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * A unique identifier for an active MCP-over-ACP connection.
 */
export const McpConnectionId = S.String
export type McpConnectionId = typeof McpConnectionId.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Request parameters for `mcp/message`.
 */
export const MessageMcpRequest = S.Struct({
  connectionId: McpConnectionId,
  method: S.String,
  params: S.optional(S.NullOr(JsonObject)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type MessageMcpRequest = typeof MessageMcpRequest.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Request parameters for `mcp/disconnect`.
 */
export const DisconnectMcpRequest = S.Struct({
  connectionId: McpConnectionId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DisconnectMcpRequest = typeof DisconnectMcpRequest.Type

/**
 * Allows for sending an arbitrary request that is not part of the ACP spec.
 * Extension methods provide a way to add custom functionality while maintaining
 * protocol compatibility.
 *
 * See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
 */
export const ExtRequest = S.Union(JsonValue)
export type ExtRequest = typeof ExtRequest.Type

/**
 * A JSON-RPC request object.
 */
export const AgentRequest = S.Struct({
  id: RequestId,
  method: S.String,
  params: S.optional(
    S.NullOr(
      S.Union(
        WriteTextFileRequest,
        ReadTextFileRequest,
        RequestPermissionRequest,
        CreateTerminalRequest,
        TerminalOutputRequest,
        ReleaseTerminalRequest,
        WaitForTerminalExitRequest,
        KillTerminalRequest,
        CreateElicitationRequest,
        ConnectMcpRequest,
        MessageMcpRequest,
        DisconnectMcpRequest,
        ExtRequest,
      ),
    ),
  ),
})
export type AgentRequest = typeof AgentRequest.Type

/**
 * Protocol version identifier.
 *
 * This version is only bumped for breaking changes.
 * Non-breaking changes should be introduced via capabilities.
 */
export const ProtocolVersion = S.Int.pipe(S.between(0, 65535))
export type ProtocolVersion = typeof ProtocolVersion.Type

/**
 * Prompt capabilities supported by the agent in `session/prompt` requests.
 *
 * Baseline agent functionality requires support for [`ContentBlock::Text`]
 * and [`ContentBlock::ResourceLink`] in prompt requests.
 *
 * Other variants must be explicitly opted in to.
 * Capabilities for different types of content in prompt requests.
 *
 * Indicates which content types beyond the baseline (text and resource links)
 * the agent can process.
 *
 * See protocol docs: [Prompt Capabilities](https://agentclientprotocol.com/protocol/initialization#prompt-capabilities)
 */
export const PromptCapabilities = S.Struct({
  image: S.optional(S.Boolean),
  audio: S.optional(S.Boolean),
  embeddedContext: S.optional(S.Boolean),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PromptCapabilities = typeof PromptCapabilities.Type

/**
 * MCP capabilities supported by the agent
 */
export const McpCapabilities = S.Struct({
  http: S.optional(S.Boolean),
  sse: S.optional(S.Boolean),
  acp: S.optional(S.Boolean),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type McpCapabilities = typeof McpCapabilities.Type

/**
 * Capabilities for the `session/list` method.
 *
 * Supplying `{}` means the agent supports listing sessions.
 */
export const SessionListCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionListCapabilities = typeof SessionListCapabilities.Type

/**
 * Capabilities for the `session/delete` method.
 *
 * Supplying `{}` means the agent supports deleting sessions from `session/list`.
 */
export const SessionDeleteCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionDeleteCapabilities = typeof SessionDeleteCapabilities.Type

/**
 * Capabilities for additional session directories support.
 *
 * Supplying `{}` means the agent supports the `additionalDirectories` field on
 * supported session lifecycle requests. Agents that also support
 * `session/list` may return `SessionInfo.additionalDirectories` to report the
 * complete ordered additional-root list associated with a listed session.
 */
export const SessionAdditionalDirectoriesCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionAdditionalDirectoriesCapabilities = typeof SessionAdditionalDirectoriesCapabilities.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Capabilities for the `session/fork` method.
 *
 * Supplying `{}` means the agent supports forking sessions.
 */
export const SessionForkCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionForkCapabilities = typeof SessionForkCapabilities.Type

/**
 * Capabilities for the `session/resume` method.
 *
 * Supplying `{}` means the agent supports resuming sessions.
 */
export const SessionResumeCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionResumeCapabilities = typeof SessionResumeCapabilities.Type

/**
 * Capabilities for the `session/close` method.
 *
 * Supplying `{}` means the agent supports closing sessions.
 */
export const SessionCloseCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionCloseCapabilities = typeof SessionCloseCapabilities.Type

/**
 * Session capabilities supported by the agent.
 *
 * As a baseline, all Agents **MUST** support `session/new`, `session/prompt`, `session/cancel`, and `session/update`.
 *
 * Optionally, they **MAY** support other session methods and notifications by specifying additional capabilities.
 *
 * Note: `session/load` is still handled by the top-level `load_session` capability. This will be unified in future versions of the protocol.
 *
 * See protocol docs: [Session Capabilities](https://agentclientprotocol.com/protocol/initialization#session-capabilities)
 */
export const SessionCapabilities = S.Struct({
  list: S.optional(S.NullOr(SessionListCapabilities)),
  delete: S.optional(S.NullOr(SessionDeleteCapabilities)),
  additionalDirectories: S.optional(S.NullOr(SessionAdditionalDirectoriesCapabilities)),
  fork: S.optional(S.NullOr(SessionForkCapabilities)),
  resume: S.optional(S.NullOr(SessionResumeCapabilities)),
  close: S.optional(S.NullOr(SessionCloseCapabilities)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionCapabilities = typeof SessionCapabilities.Type

/**
 * Logout capabilities supported by the agent.
 *
 * Supplying `{}` means the agent supports the logout method.
 */
export const LogoutCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type LogoutCapabilities = typeof LogoutCapabilities.Type

/**
 * Authentication-related capabilities supported by the agent.
 */
export const AgentAuthCapabilities = S.Struct({
  logout: S.optional(S.NullOr(LogoutCapabilities)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AgentAuthCapabilities = typeof AgentAuthCapabilities.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Provider configuration capabilities supported by the agent.
 *
 * Supplying `{}` means the agent supports provider configuration methods.
 */
export const ProvidersCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ProvidersCapabilities = typeof ProvidersCapabilities.Type

/**
 * Marker for `document/didOpen` capability support.
 */
export const NesDocumentDidOpenCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesDocumentDidOpenCapabilities = typeof NesDocumentDidOpenCapabilities.Type

/**
 * How the agent wants document changes delivered.
 */
export const TextDocumentSyncKind = S.Literal('full', 'incremental')
export type TextDocumentSyncKind = typeof TextDocumentSyncKind.Type

/**
 * Capabilities for `document/didChange` events.
 */
export const NesDocumentDidChangeCapabilities = S.Struct({
  syncKind: TextDocumentSyncKind,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesDocumentDidChangeCapabilities = typeof NesDocumentDidChangeCapabilities.Type

/**
 * Marker for `document/didClose` capability support.
 */
export const NesDocumentDidCloseCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesDocumentDidCloseCapabilities = typeof NesDocumentDidCloseCapabilities.Type

/**
 * Marker for `document/didSave` capability support.
 */
export const NesDocumentDidSaveCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesDocumentDidSaveCapabilities = typeof NesDocumentDidSaveCapabilities.Type

/**
 * Marker for `document/didFocus` capability support.
 */
export const NesDocumentDidFocusCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesDocumentDidFocusCapabilities = typeof NesDocumentDidFocusCapabilities.Type

/**
 * Document event capabilities the agent wants to receive.
 */
export const NesDocumentEventCapabilities = S.Struct({
  didOpen: S.optional(S.NullOr(NesDocumentDidOpenCapabilities)),
  didChange: S.optional(S.NullOr(NesDocumentDidChangeCapabilities)),
  didClose: S.optional(S.NullOr(NesDocumentDidCloseCapabilities)),
  didSave: S.optional(S.NullOr(NesDocumentDidSaveCapabilities)),
  didFocus: S.optional(S.NullOr(NesDocumentDidFocusCapabilities)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesDocumentEventCapabilities = typeof NesDocumentEventCapabilities.Type

/**
 * Event capabilities the agent can consume.
 */
export const NesEventCapabilities = S.Struct({
  document: S.optional(S.NullOr(NesDocumentEventCapabilities)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesEventCapabilities = typeof NesEventCapabilities.Type

/**
 * Capabilities for recent files context.
 */
export const NesRecentFilesCapabilities = S.Struct({
  maxCount: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesRecentFilesCapabilities = typeof NesRecentFilesCapabilities.Type

/**
 * Capabilities for related snippets context.
 */
export const NesRelatedSnippetsCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesRelatedSnippetsCapabilities = typeof NesRelatedSnippetsCapabilities.Type

/**
 * Capabilities for edit history context.
 */
export const NesEditHistoryCapabilities = S.Struct({
  maxCount: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesEditHistoryCapabilities = typeof NesEditHistoryCapabilities.Type

/**
 * Capabilities for user actions context.
 */
export const NesUserActionsCapabilities = S.Struct({
  maxCount: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesUserActionsCapabilities = typeof NesUserActionsCapabilities.Type

/**
 * Capabilities for open files context.
 */
export const NesOpenFilesCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesOpenFilesCapabilities = typeof NesOpenFilesCapabilities.Type

/**
 * Capabilities for diagnostics context.
 */
export const NesDiagnosticsCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesDiagnosticsCapabilities = typeof NesDiagnosticsCapabilities.Type

/**
 * Context capabilities the agent wants attached to each suggestion request.
 */
export const NesContextCapabilities = S.Struct({
  recentFiles: S.optional(S.NullOr(NesRecentFilesCapabilities)),
  relatedSnippets: S.optional(S.NullOr(NesRelatedSnippetsCapabilities)),
  editHistory: S.optional(S.NullOr(NesEditHistoryCapabilities)),
  userActions: S.optional(S.NullOr(NesUserActionsCapabilities)),
  openFiles: S.optional(S.NullOr(NesOpenFilesCapabilities)),
  diagnostics: S.optional(S.NullOr(NesDiagnosticsCapabilities)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesContextCapabilities = typeof NesContextCapabilities.Type

/**
 * NES capabilities advertised by the agent during initialization.
 */
export const NesCapabilities = S.Struct({
  events: S.optional(S.NullOr(NesEventCapabilities)),
  context: S.optional(S.NullOr(NesContextCapabilities)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesCapabilities = typeof NesCapabilities.Type

/**
 * The encoding used for character offsets in positions.
 *
 * Follows the same conventions as LSP 3.17. The default is UTF-16.
 */
export const PositionEncodingKind = S.Literal('utf-16', 'utf-32', 'utf-8')
export type PositionEncodingKind = typeof PositionEncodingKind.Type

/**
 * Capabilities supported by the agent.
 *
 * Advertised during initialization to inform the client about
 * available features and content types.
 *
 * See protocol docs: [Agent Capabilities](https://agentclientprotocol.com/protocol/initialization#agent-capabilities)
 */
export const AgentCapabilities = S.Struct({
  loadSession: S.optional(S.Boolean),
  promptCapabilities: S.optional(PromptCapabilities),
  mcpCapabilities: S.optional(McpCapabilities),
  sessionCapabilities: S.optional(SessionCapabilities),
  auth: S.optional(AgentAuthCapabilities),
  providers: S.optional(S.NullOr(ProvidersCapabilities)),
  nes: S.optional(S.NullOr(NesCapabilities)),
  positionEncoding: S.optional(S.NullOr(PositionEncodingKind)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AgentCapabilities = typeof AgentCapabilities.Type

/**
 * Typed identifier used for auth method values on the wire.
 */
export const AuthMethodId = S.String
export type AuthMethodId = typeof AuthMethodId.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Describes a single environment variable for an [`AuthMethodEnvVar`] authentication method.
 */
export const AuthEnvVar = S.Struct({
  name: S.String,
  label: S.optional(S.NullOr(S.String)),
  secret: S.optional(S.Boolean),
  optional: S.optional(S.Boolean),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AuthEnvVar = typeof AuthEnvVar.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Environment variable authentication method.
 *
 * The user provides credentials that the client passes to the agent as environment variables.
 */
export const AuthMethodEnvVar = S.Struct({
  id: AuthMethodId,
  name: S.String,
  description: S.optional(S.NullOr(S.String)),
  vars: S.Array(AuthEnvVar),
  link: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AuthMethodEnvVar = typeof AuthMethodEnvVar.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Terminal-based authentication method.
 *
 * The client runs an interactive terminal for the user to authenticate via a TUI.
 */
export const AuthMethodTerminal = S.Struct({
  id: AuthMethodId,
  name: S.String,
  description: S.optional(S.NullOr(S.String)),
  args: S.optional(S.Array(S.String)),
  env: S.optional(S.Record({ key: JsonObjectKey, value: S.String })),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AuthMethodTerminal = typeof AuthMethodTerminal.Type

/**
 * Agent handles authentication itself.
 *
 * This is the default authentication method type.
 */
export const AuthMethodAgent = S.Struct({
  id: AuthMethodId,
  name: S.String,
  description: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AuthMethodAgent = typeof AuthMethodAgent.Type

/**
 * Describes an available authentication method.
 *
 * The `type` field acts as the discriminator in the serialized JSON form.
 * When no `type` is present, the method is treated as `agent`.
 */
export const AuthMethod = S.Union(
  S.extend(
    AuthMethodEnvVar,
    S.Struct({
      type: S.Literal('env_var'),
    }),
  ),
  S.extend(
    AuthMethodTerminal,
    S.Struct({
      type: S.Literal('terminal'),
    }),
  ),
  AuthMethodAgent,
)
export type AuthMethod = typeof AuthMethod.Type

/**
 * Metadata about the implementation of the client or agent.
 * Describes the name and version of an ACP implementation, with an optional
 * title for UI representation.
 */
export const Implementation = S.Struct({
  name: S.String,
  title: S.optional(S.NullOr(S.String)),
  version: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type Implementation = typeof Implementation.Type

/**
 * Response to the `initialize` method.
 *
 * Contains the negotiated protocol version and agent capabilities.
 *
 * See protocol docs: [Initialization](https://agentclientprotocol.com/protocol/initialization)
 */
export const InitializeResponse = S.Struct({
  protocolVersion: ProtocolVersion,
  agentCapabilities: S.optional(AgentCapabilities),
  authMethods: S.optional(S.Array(AuthMethod)),
  agentInfo: S.optional(S.NullOr(Implementation)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type InitializeResponse = typeof InitializeResponse.Type

/**
 * Response to the `authenticate` method.
 */
export const AuthenticateResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AuthenticateResponse = typeof AuthenticateResponse.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Unique identifier for a configurable LLM provider.
 */
export const ProviderId = S.String
export type ProviderId = typeof ProviderId.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Well-known API protocol identifiers for LLM providers.
 *
 * Agents and clients MUST handle unknown protocol identifiers gracefully.
 *
 * Protocol names beginning with `_` are free for custom use, like other ACP extension methods.
 * Protocol names that do not begin with `_` are reserved for the ACP spec.
 */
export const LlmProtocol = S.Union(
  S.Literal('anthropic'),
  S.Literal('openai'),
  S.Literal('azure'),
  S.Literal('vertex'),
  S.Literal('bedrock'),
  S.String,
)
export type LlmProtocol = typeof LlmProtocol.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Current effective non-secret routing configuration for a provider.
 */
export const ProviderCurrentConfig = S.Struct({
  apiType: LlmProtocol,
  baseUrl: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ProviderCurrentConfig = typeof ProviderCurrentConfig.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Information about a configurable LLM provider.
 */
export const ProviderInfo = S.Struct({
  providerId: ProviderId,
  supported: S.Array(LlmProtocol),
  required: S.Boolean,
  current: S.optional(S.NullOr(ProviderCurrentConfig)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ProviderInfo = typeof ProviderInfo.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Response to `providers/list`.
 */
export const ListProvidersResponse = S.Struct({
  providers: S.Array(ProviderInfo),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ListProvidersResponse = typeof ListProvidersResponse.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Response to `providers/set`.
 */
export const SetProviderResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SetProviderResponse = typeof SetProviderResponse.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Response to `providers/disable`.
 */
export const DisableProviderResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DisableProviderResponse = typeof DisableProviderResponse.Type

/**
 * Response to the `logout` method.
 */
export const LogoutResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type LogoutResponse = typeof LogoutResponse.Type

/**
 * Unique identifier for a Session Mode.
 */
export const SessionModeId = S.String
export type SessionModeId = typeof SessionModeId.Type

/**
 * A mode the agent can operate in.
 *
 * See protocol docs: [Session Modes](https://agentclientprotocol.com/protocol/session-modes)
 */
export const SessionMode = S.Struct({
  id: SessionModeId,
  name: S.String,
  description: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionMode = typeof SessionMode.Type

/**
 * The set of modes and the one currently active.
 */
export const SessionModeState = S.Struct({
  currentModeId: SessionModeId,
  availableModes: S.Array(SessionMode),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionModeState = typeof SessionModeState.Type

/**
 * Unique identifier for a session configuration option.
 */
export const SessionConfigId = S.String
export type SessionConfigId = typeof SessionConfigId.Type

/**
 * Semantic category for a session configuration option.
 *
 * This is intended to help Clients distinguish broadly common selectors (e.g. model selector vs
 * session mode selector vs thought/reasoning level) for UX purposes (keyboard shortcuts, icons,
 * placement). It MUST NOT be required for correctness. Clients MUST handle missing or unknown
 * categories gracefully.
 *
 * Category names beginning with `_` are free for custom use, like other ACP extension methods.
 * Category names that do not begin with `_` are reserved for the ACP spec.
 */
export const SessionConfigOptionCategory = S.Union(
  S.Literal('mode'),
  S.Literal('model'),
  S.Literal('model_config'),
  S.Literal('thought_level'),
  S.String,
)
export type SessionConfigOptionCategory = typeof SessionConfigOptionCategory.Type

/**
 * Unique identifier for a session configuration option value.
 */
export const SessionConfigValueId = S.String
export type SessionConfigValueId = typeof SessionConfigValueId.Type

/**
 * A possible value for a session configuration option.
 */
export const SessionConfigSelectOption = S.Struct({
  value: SessionConfigValueId,
  name: S.String,
  description: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionConfigSelectOption = typeof SessionConfigSelectOption.Type

/**
 * Unique identifier for a session configuration option value group.
 */
export const SessionConfigGroupId = S.String
export type SessionConfigGroupId = typeof SessionConfigGroupId.Type

/**
 * A group of possible values for a session configuration option.
 */
export const SessionConfigSelectGroup = S.Struct({
  group: SessionConfigGroupId,
  name: S.String,
  options: S.Array(SessionConfigSelectOption),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionConfigSelectGroup = typeof SessionConfigSelectGroup.Type

/**
 * Possible values for a session configuration option.
 */
export const SessionConfigSelectOptions = S.Union(S.Array(SessionConfigSelectOption), S.Array(SessionConfigSelectGroup))
export type SessionConfigSelectOptions = typeof SessionConfigSelectOptions.Type

/**
 * A single-value selector (dropdown) session configuration option payload.
 */
export const SessionConfigSelect = S.Struct({
  currentValue: SessionConfigValueId,
  options: SessionConfigSelectOptions,
})
export type SessionConfigSelect = typeof SessionConfigSelect.Type

/**
 * A boolean on/off toggle session configuration option payload.
 */
export const SessionConfigBoolean = S.Struct({
  currentValue: S.Boolean,
})
export type SessionConfigBoolean = typeof SessionConfigBoolean.Type

/**
 * A session configuration option selector and its current state.
 */
export const SessionConfigOption = S.Union(
  S.extend(
    SessionConfigSelect,
    S.Struct({
      type: S.Literal('select'),
    }),
  ),
  S.extend(
    SessionConfigBoolean,
    S.Struct({
      type: S.Literal('boolean'),
    }),
  ),
)
export type SessionConfigOption = typeof SessionConfigOption.Type

/**
 * Response from creating a new session.
 *
 * See protocol docs: [Creating a Session](https://agentclientprotocol.com/protocol/session-setup#creating-a-session)
 */
export const NewSessionResponse = S.Struct({
  sessionId: SessionId,
  modes: S.optional(S.NullOr(SessionModeState)),
  configOptions: S.optional(S.NullOr(S.Array(SessionConfigOption))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NewSessionResponse = typeof NewSessionResponse.Type

/**
 * Response from loading an existing session.
 */
export const LoadSessionResponse = S.Struct({
  modes: S.optional(S.NullOr(SessionModeState)),
  configOptions: S.optional(S.NullOr(S.Array(SessionConfigOption))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type LoadSessionResponse = typeof LoadSessionResponse.Type

/**
 * Information about a session returned by session/list
 */
export const SessionInfo = S.Struct({
  sessionId: SessionId,
  cwd: S.String,
  additionalDirectories: S.optional(S.Array(S.String)),
  title: S.optional(S.NullOr(S.String)),
  updatedAt: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionInfo = typeof SessionInfo.Type

/**
 * Response from listing sessions.
 */
export const ListSessionsResponse = S.Struct({
  sessions: S.Array(SessionInfo),
  nextCursor: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ListSessionsResponse = typeof ListSessionsResponse.Type

/**
 * Response from deleting a session.
 */
export const DeleteSessionResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DeleteSessionResponse = typeof DeleteSessionResponse.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Response from forking an existing session.
 */
export const ForkSessionResponse = S.Struct({
  sessionId: SessionId,
  modes: S.optional(S.NullOr(SessionModeState)),
  configOptions: S.optional(S.NullOr(S.Array(SessionConfigOption))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ForkSessionResponse = typeof ForkSessionResponse.Type

/**
 * Response from resuming an existing session.
 */
export const ResumeSessionResponse = S.Struct({
  modes: S.optional(S.NullOr(SessionModeState)),
  configOptions: S.optional(S.NullOr(S.Array(SessionConfigOption))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ResumeSessionResponse = typeof ResumeSessionResponse.Type

/**
 * Response from closing a session.
 */
export const CloseSessionResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type CloseSessionResponse = typeof CloseSessionResponse.Type

/**
 * Response to `session/set_mode` method.
 */
export const SetSessionModeResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SetSessionModeResponse = typeof SetSessionModeResponse.Type

/**
 * Response to `session/set_config_option` method.
 */
export const SetSessionConfigOptionResponse = S.Struct({
  configOptions: S.Array(SessionConfigOption),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SetSessionConfigOptionResponse = typeof SetSessionConfigOptionResponse.Type

/**
 * Reasons why an agent stops processing a prompt turn.
 *
 * See protocol docs: [Stop Reasons](https://agentclientprotocol.com/protocol/prompt-turn#stop-reasons)
 */
export const StopReason = S.Literal('end_turn', 'max_tokens', 'max_turn_requests', 'refusal', 'cancelled')
export type StopReason = typeof StopReason.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Token usage information for a prompt turn.
 */
export const Usage = S.Struct({
  totalTokens: S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)),
  inputTokens: S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)),
  outputTokens: S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)),
  thoughtTokens: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  cachedReadTokens: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  cachedWriteTokens: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type Usage = typeof Usage.Type

/**
 * Response from processing a user prompt.
 *
 * See protocol docs: [Check for Completion](https://agentclientprotocol.com/protocol/prompt-turn#4-check-for-completion)
 */
export const PromptResponse = S.Struct({
  stopReason: StopReason,
  usage: S.optional(S.NullOr(Usage)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PromptResponse = typeof PromptResponse.Type

/**
 * Response to `nes/start`.
 */
export const StartNesResponse = S.Struct({
  sessionId: SessionId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type StartNesResponse = typeof StartNesResponse.Type

/**
 * Unique identifier for a next edit suggestion.
 */
export const NesSuggestionId = S.String
export type NesSuggestionId = typeof NesSuggestionId.Type

/**
 * A zero-based position in a text document.
 *
 * The meaning of `character` depends on the negotiated position encoding.
 */
export const Position = S.Struct({
  line: S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)),
  character: S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type Position = typeof Position.Type

/**
 * A range in a text document, expressed as start and end positions.
 */
export const Range = S.Struct({
  start: Position,
  end: Position,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type Range = typeof Range.Type

/**
 * A text edit within a suggestion.
 */
export const NesTextEdit = S.Struct({
  range: Range,
  newText: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesTextEdit = typeof NesTextEdit.Type

/**
 * A text edit suggestion.
 */
export const NesEditSuggestion = S.Struct({
  id: NesSuggestionId,
  uri: S.String,
  edits: S.Array(NesTextEdit),
  cursorPosition: S.optional(S.NullOr(Position)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesEditSuggestion = typeof NesEditSuggestion.Type

/**
 * A jump-to-location suggestion.
 */
export const NesJumpSuggestion = S.Struct({
  id: NesSuggestionId,
  uri: S.String,
  position: Position,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesJumpSuggestion = typeof NesJumpSuggestion.Type

/**
 * A rename symbol suggestion.
 */
export const NesRenameSuggestion = S.Struct({
  id: NesSuggestionId,
  uri: S.String,
  position: Position,
  newName: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesRenameSuggestion = typeof NesRenameSuggestion.Type

/**
 * A search-and-replace suggestion.
 */
export const NesSearchAndReplaceSuggestion = S.Struct({
  id: NesSuggestionId,
  uri: S.String,
  search: S.String,
  replace: S.String,
  isRegex: S.optional(S.NullOr(S.Boolean)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesSearchAndReplaceSuggestion = typeof NesSearchAndReplaceSuggestion.Type

/**
 * A suggestion returned by the agent.
 */
export const NesSuggestion = S.Union(
  S.extend(
    NesEditSuggestion,
    S.Struct({
      kind: S.Literal('edit'),
    }),
  ),
  S.extend(
    NesJumpSuggestion,
    S.Struct({
      kind: S.Literal('jump'),
    }),
  ),
  S.extend(
    NesRenameSuggestion,
    S.Struct({
      kind: S.Literal('rename'),
    }),
  ),
  S.extend(
    NesSearchAndReplaceSuggestion,
    S.Struct({
      kind: S.Literal('searchAndReplace'),
    }),
  ),
)
export type NesSuggestion = typeof NesSuggestion.Type

/**
 * Response to `nes/suggest`.
 */
export const SuggestNesResponse = S.Struct({
  suggestions: S.Array(NesSuggestion),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SuggestNesResponse = typeof SuggestNesResponse.Type

/**
 * Response from closing an NES session.
 */
export const CloseNesResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type CloseNesResponse = typeof CloseNesResponse.Type

/**
 * Allows for sending an arbitrary response to an [`ExtRequest`] that is not part of the ACP spec.
 * Extension methods provide a way to add custom functionality while maintaining
 * protocol compatibility.
 *
 * See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
 */
export const ExtResponse = S.Union(JsonValue)
export type ExtResponse = typeof ExtResponse.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Response to `mcp/message`.
 *
 * This is the inner MCP response result payload. Any JSON value is valid.
 */
export const MessageMcpResponse = S.Union(JsonValue)
export type MessageMcpResponse = typeof MessageMcpResponse.Type

/**
 * Predefined error codes for common JSON-RPC and ACP-specific errors.
 *
 * These codes follow the JSON-RPC 2.0 specification for standard errors
 * and use the reserved range (-32000 to -32099) for protocol-specific errors.
 */
export const ErrorCode = S.Union(
  S.Literal(-32700),
  S.Literal(-32600),
  S.Literal(-32601),
  S.Literal(-32602),
  S.Literal(-32603),
  S.Literal(-32800),
  S.Literal(-32000),
  S.Literal(-32002),
  S.Int,
)
export type ErrorCode = typeof ErrorCode.Type

/**
 * JSON-RPC error object.
 *
 * Represents an error that occurred during method execution, following the
 * JSON-RPC 2.0 error object specification with optional additional data.
 *
 * See protocol docs: [JSON-RPC Error Object](https://www.jsonrpc.org/specification#error_object)
 */
export const Error = S.Struct({
  code: ErrorCode,
  message: S.String,
  data: S.optional(JsonValue),
})
export type Error = typeof Error.Type

/**
 * A JSON-RPC response object.
 */
export const AgentResponse = S.Union(
  S.Struct({
    id: RequestId,
    result: S.Union(
      InitializeResponse,
      AuthenticateResponse,
      ListProvidersResponse,
      SetProviderResponse,
      DisableProviderResponse,
      LogoutResponse,
      NewSessionResponse,
      LoadSessionResponse,
      ListSessionsResponse,
      DeleteSessionResponse,
      ForkSessionResponse,
      ResumeSessionResponse,
      CloseSessionResponse,
      SetSessionModeResponse,
      SetSessionConfigOptionResponse,
      PromptResponse,
      StartNesResponse,
      SuggestNesResponse,
      CloseNesResponse,
      ExtResponse,
      MessageMcpResponse,
    ),
  }),
  S.Struct({
    id: RequestId,
    error: Error,
  }),
)
export type AgentResponse = typeof AgentResponse.Type

/**
 * Unique identifier for a message within a session.
 */
export const MessageId = S.String
export type MessageId = typeof MessageId.Type

/**
 * A streamed item of content
 */
export const ContentChunk = S.Struct({
  content: ContentBlock,
  messageId: S.optional(S.NullOr(MessageId)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ContentChunk = typeof ContentChunk.Type

/**
 * Represents a tool call that the language model has requested.
 *
 * Tool calls are actions that the agent executes on behalf of the language model,
 * such as reading files, executing code, or fetching data from external sources.
 *
 * See protocol docs: [Tool Calls](https://agentclientprotocol.com/protocol/tool-calls)
 */
export const ToolCall = S.Struct({
  toolCallId: ToolCallId,
  title: S.String,
  name: S.optional(S.NullOr(S.String)),
  kind: S.optional(ToolKind),
  status: S.optional(ToolCallStatus),
  content: S.optional(S.Array(ToolCallContent)),
  locations: S.optional(S.Array(ToolCallLocation)),
  rawInput: S.optional(JsonValue),
  rawOutput: S.optional(JsonValue),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ToolCall = typeof ToolCall.Type

/**
 * Priority levels for plan entries.
 *
 * Used to indicate the relative importance or urgency of different
 * tasks in the execution plan.
 * See protocol docs: [Plan Entries](https://agentclientprotocol.com/protocol/agent-plan#plan-entries)
 */
export const PlanEntryPriority = S.Literal('high', 'medium', 'low')
export type PlanEntryPriority = typeof PlanEntryPriority.Type

/**
 * Status of a plan entry in the execution flow.
 *
 * Tracks the lifecycle of each task from planning through completion.
 * See protocol docs: [Plan Entries](https://agentclientprotocol.com/protocol/agent-plan#plan-entries)
 */
export const PlanEntryStatus = S.Literal('pending', 'in_progress', 'completed')
export type PlanEntryStatus = typeof PlanEntryStatus.Type

/**
 * A single entry in the execution plan.
 *
 * Represents a task or goal that the assistant intends to accomplish
 * as part of fulfilling the user's request.
 * See protocol docs: [Plan Entries](https://agentclientprotocol.com/protocol/agent-plan#plan-entries)
 */
export const PlanEntry = S.Struct({
  content: S.String,
  priority: PlanEntryPriority,
  status: PlanEntryStatus,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PlanEntry = typeof PlanEntry.Type

/**
 * An execution plan for accomplishing complex tasks.
 *
 * Plans consist of multiple entries representing individual tasks or goals.
 * Agents report plans to clients to provide visibility into their execution strategy.
 * Plans can evolve during execution as the agent discovers new requirements or completes tasks.
 *
 * See protocol docs: [Agent Plan](https://agentclientprotocol.com/protocol/agent-plan)
 */
export const Plan = S.Struct({
  entries: S.Array(PlanEntry),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type Plan = typeof Plan.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Unique identifier for a plan within a session.
 */
export const PlanId = S.String
export type PlanId = typeof PlanId.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * A plan represented as structured entries.
 */
export const PlanItems = S.Struct({
  planId: PlanId,
  entries: S.Array(PlanEntry),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PlanItems = typeof PlanItems.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * A plan represented by a file URI.
 */
export const PlanFile = S.Struct({
  planId: PlanId,
  uri: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PlanFile = typeof PlanFile.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * A plan represented as raw markdown content.
 */
export const PlanMarkdown = S.Struct({
  planId: PlanId,
  content: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PlanMarkdown = typeof PlanMarkdown.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Updated content for a plan.
 */
export const PlanUpdateContent = S.Union(
  S.extend(
    PlanItems,
    S.Struct({
      type: S.Literal('items'),
    }),
  ),
  S.extend(
    PlanFile,
    S.Struct({
      type: S.Literal('file'),
    }),
  ),
  S.extend(
    PlanMarkdown,
    S.Struct({
      type: S.Literal('markdown'),
    }),
  ),
)
export type PlanUpdateContent = typeof PlanUpdateContent.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * A content update for a plan identified by ID.
 */
export const PlanUpdate = S.Struct({
  plan: PlanUpdateContent,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PlanUpdate = typeof PlanUpdate.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Removal notice for a plan identified by ID.
 */
export const PlanRemoved = S.Struct({
  planId: PlanId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PlanRemoved = typeof PlanRemoved.Type

/**
 * All text that was typed after the command name is provided as input.
 */
export const UnstructuredCommandInput = S.Struct({
  hint: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type UnstructuredCommandInput = typeof UnstructuredCommandInput.Type

/**
 * The input specification for a command.
 */
export const AvailableCommandInput = S.Union(UnstructuredCommandInput)
export type AvailableCommandInput = typeof AvailableCommandInput.Type

/**
 * Information about a command.
 */
export const AvailableCommand = S.Struct({
  name: S.String,
  description: S.String,
  input: S.optional(S.NullOr(AvailableCommandInput)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AvailableCommand = typeof AvailableCommand.Type

/**
 * Available commands are ready or have changed
 */
export const AvailableCommandsUpdate = S.Struct({
  availableCommands: S.Array(AvailableCommand),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AvailableCommandsUpdate = typeof AvailableCommandsUpdate.Type

/**
 * The current mode of the session has changed
 *
 * See protocol docs: [Session Modes](https://agentclientprotocol.com/protocol/session-modes)
 */
export const CurrentModeUpdate = S.Struct({
  currentModeId: SessionModeId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type CurrentModeUpdate = typeof CurrentModeUpdate.Type

/**
 * Session configuration options have been updated.
 */
export const ConfigOptionUpdate = S.Struct({
  configOptions: S.Array(SessionConfigOption),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ConfigOptionUpdate = typeof ConfigOptionUpdate.Type

/**
 * Update to session metadata. All fields are optional to support partial updates.
 *
 * Agents send this notification to update session information like title or custom metadata.
 * This allows clients to display dynamic session names and track session state changes.
 */
export const SessionInfoUpdate = S.Struct({
  title: S.optional(S.NullOr(S.String)),
  updatedAt: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionInfoUpdate = typeof SessionInfoUpdate.Type

/**
 * Cost information for a session.
 */
export const Cost = S.Struct({
  amount: S.JsonNumber,
  currency: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type Cost = typeof Cost.Type

/**
 * Context window and cost update for a session.
 */
export const UsageUpdate = S.Struct({
  used: S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)),
  size: S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)),
  cost: S.optional(S.NullOr(Cost)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type UsageUpdate = typeof UsageUpdate.Type

/**
 * Different types of updates that can be sent during session processing.
 *
 * These updates provide real-time feedback about the agent's progress.
 *
 * See protocol docs: [Agent Reports Output](https://agentclientprotocol.com/protocol/prompt-turn#3-agent-reports-output)
 */
export const SessionUpdate = S.Union(
  S.extend(
    ContentChunk,
    S.Struct({
      sessionUpdate: S.Literal('user_message_chunk'),
    }),
  ),
  S.extend(
    ContentChunk,
    S.Struct({
      sessionUpdate: S.Literal('agent_message_chunk'),
    }),
  ),
  S.extend(
    ContentChunk,
    S.Struct({
      sessionUpdate: S.Literal('agent_thought_chunk'),
    }),
  ),
  S.extend(
    ToolCall,
    S.Struct({
      sessionUpdate: S.Literal('tool_call'),
    }),
  ),
  S.extend(
    ToolCallUpdate,
    S.Struct({
      sessionUpdate: S.Literal('tool_call_update'),
    }),
  ),
  S.extend(
    Plan,
    S.Struct({
      sessionUpdate: S.Literal('plan'),
    }),
  ),
  S.extend(
    PlanUpdate,
    S.Struct({
      sessionUpdate: S.Literal('plan_update'),
    }),
  ),
  S.extend(
    PlanRemoved,
    S.Struct({
      sessionUpdate: S.Literal('plan_removed'),
    }),
  ),
  S.extend(
    AvailableCommandsUpdate,
    S.Struct({
      sessionUpdate: S.Literal('available_commands_update'),
    }),
  ),
  S.extend(
    CurrentModeUpdate,
    S.Struct({
      sessionUpdate: S.Literal('current_mode_update'),
    }),
  ),
  S.extend(
    ConfigOptionUpdate,
    S.Struct({
      sessionUpdate: S.Literal('config_option_update'),
    }),
  ),
  S.extend(
    SessionInfoUpdate,
    S.Struct({
      sessionUpdate: S.Literal('session_info_update'),
    }),
  ),
  S.extend(
    UsageUpdate,
    S.Struct({
      sessionUpdate: S.Literal('usage_update'),
    }),
  ),
)
export type SessionUpdate = typeof SessionUpdate.Type

/**
 * Notification containing a session update from the agent.
 *
 * Used to stream real-time progress and results during prompt processing.
 *
 * See protocol docs: [Agent Reports Output](https://agentclientprotocol.com/protocol/prompt-turn#3-agent-reports-output)
 */
export const SessionNotification = S.Struct({
  sessionId: SessionId,
  update: SessionUpdate,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionNotification = typeof SessionNotification.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Notification sent by the agent when a URL-based elicitation is complete.
 */
export const CompleteElicitationNotification = S.Struct({
  elicitationId: ElicitationId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type CompleteElicitationNotification = typeof CompleteElicitationNotification.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Notification parameters for `mcp/message`.
 *
 * This is used when the wrapped MCP message is a notification and the outer JSON-RPC
 * envelope has no `id`.
 */
export const MessageMcpNotification = S.Struct({
  connectionId: McpConnectionId,
  method: S.String,
  params: S.optional(S.NullOr(JsonObject)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type MessageMcpNotification = typeof MessageMcpNotification.Type

/**
 * Allows the Agent to send an arbitrary notification that is not part of the ACP spec.
 * Extension notifications provide a way to send one-way messages for custom functionality
 * while maintaining protocol compatibility.
 *
 * See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/extensibility)
 */
export const ExtNotification = S.Union(JsonValue)
export type ExtNotification = typeof ExtNotification.Type

/**
 * A JSON-RPC notification object.
 */
export const AgentNotification = S.Struct({
  method: S.String,
  params: S.optional(
    S.NullOr(S.Union(SessionNotification, CompleteElicitationNotification, MessageMcpNotification, ExtNotification)),
  ),
})
export type AgentNotification = typeof AgentNotification.Type

/**
 * File system capabilities that a client may support.
 *
 * See protocol docs: [FileSystem](https://agentclientprotocol.com/protocol/initialization#filesystem)
 */
export const FileSystemCapabilities = S.Struct({
  readTextFile: S.optional(S.Boolean),
  writeTextFile: S.optional(S.Boolean),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type FileSystemCapabilities = typeof FileSystemCapabilities.Type

/**
 * Capabilities for boolean session configuration options.
 *
 * Supplying `{}` means the client supports boolean session configuration options.
 */
export const BooleanConfigOptionCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type BooleanConfigOptionCapabilities = typeof BooleanConfigOptionCapabilities.Type

/**
 * Session configuration option capabilities supported by the client.
 */
export const SessionConfigOptionsCapabilities = S.Struct({
  boolean: S.optional(S.NullOr(BooleanConfigOptionCapabilities)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SessionConfigOptionsCapabilities = typeof SessionConfigOptionsCapabilities.Type

/**
 * Session-related capabilities supported by the client.
 */
export const ClientSessionCapabilities = S.Struct({
  configOptions: S.optional(S.NullOr(SessionConfigOptionsCapabilities)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ClientSessionCapabilities = typeof ClientSessionCapabilities.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Capabilities for receiving `plan_update` and `plan_removed` session updates.
 */
export const PlanCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PlanCapabilities = typeof PlanCapabilities.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Authentication capabilities supported by the client.
 *
 * Advertised during initialization to inform the agent which authentication
 * method types the client can handle. This governs opt-in types that require
 * additional client-side support.
 */
export const AuthCapabilities = S.Struct({
  terminal: S.optional(S.Boolean),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AuthCapabilities = typeof AuthCapabilities.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Form-based elicitation capabilities.
 *
 * Supplying `{}` means the client supports form-based elicitation.
 */
export const ElicitationFormCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ElicitationFormCapabilities = typeof ElicitationFormCapabilities.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * URL-based elicitation capabilities.
 *
 * Supplying `{}` means the client supports URL-based elicitation.
 */
export const ElicitationUrlCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ElicitationUrlCapabilities = typeof ElicitationUrlCapabilities.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Elicitation capabilities supported by the client.
 */
export const ElicitationCapabilities = S.Struct({
  form: S.optional(S.NullOr(ElicitationFormCapabilities)),
  url: S.optional(S.NullOr(ElicitationUrlCapabilities)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ElicitationCapabilities = typeof ElicitationCapabilities.Type

/**
 * Marker for jump suggestion support.
 */
export const NesJumpCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesJumpCapabilities = typeof NesJumpCapabilities.Type

/**
 * Marker for rename suggestion support.
 */
export const NesRenameCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesRenameCapabilities = typeof NesRenameCapabilities.Type

/**
 * Marker for search and replace suggestion support.
 */
export const NesSearchAndReplaceCapabilities = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesSearchAndReplaceCapabilities = typeof NesSearchAndReplaceCapabilities.Type

/**
 * NES capabilities advertised by the client during initialization.
 */
export const ClientNesCapabilities = S.Struct({
  jump: S.optional(S.NullOr(NesJumpCapabilities)),
  rename: S.optional(S.NullOr(NesRenameCapabilities)),
  searchAndReplace: S.optional(S.NullOr(NesSearchAndReplaceCapabilities)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ClientNesCapabilities = typeof ClientNesCapabilities.Type

/**
 * Capabilities supported by the client.
 *
 * Advertised during initialization to inform the agent about
 * available features and methods.
 *
 * See protocol docs: [Client Capabilities](https://agentclientprotocol.com/protocol/initialization#client-capabilities)
 */
export const ClientCapabilities = S.Struct({
  fs: S.optional(FileSystemCapabilities),
  terminal: S.optional(S.Boolean),
  session: S.optional(S.NullOr(ClientSessionCapabilities)),
  plan: S.optional(S.NullOr(PlanCapabilities)),
  auth: S.optional(AuthCapabilities),
  elicitation: S.optional(S.NullOr(ElicitationCapabilities)),
  nes: S.optional(S.NullOr(ClientNesCapabilities)),
  positionEncodings: S.optional(S.Array(PositionEncodingKind)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ClientCapabilities = typeof ClientCapabilities.Type

/**
 * Request parameters for the initialize method.
 *
 * Sent by the client to establish connection and negotiate capabilities.
 *
 * See protocol docs: [Initialization](https://agentclientprotocol.com/protocol/initialization)
 */
export const InitializeRequest = S.Struct({
  protocolVersion: ProtocolVersion,
  clientCapabilities: S.optional(ClientCapabilities),
  clientInfo: S.optional(S.NullOr(Implementation)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type InitializeRequest = typeof InitializeRequest.Type

/**
 * Request parameters for the authenticate method.
 *
 * Specifies which authentication method to use.
 */
export const AuthenticateRequest = S.Struct({
  methodId: AuthMethodId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AuthenticateRequest = typeof AuthenticateRequest.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Request parameters for `providers/list`.
 */
export const ListProvidersRequest = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ListProvidersRequest = typeof ListProvidersRequest.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Request parameters for `providers/set`.
 *
 * Replaces the full configuration for one provider ID.
 */
export const SetProviderRequest = S.Struct({
  providerId: ProviderId,
  apiType: LlmProtocol,
  baseUrl: S.String,
  headers: S.optional(S.Record({ key: JsonObjectKey, value: S.String })),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SetProviderRequest = typeof SetProviderRequest.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Request parameters for `providers/disable`.
 */
export const DisableProviderRequest = S.Struct({
  providerId: ProviderId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DisableProviderRequest = typeof DisableProviderRequest.Type

/**
 * Request parameters for the logout method.
 *
 * Terminates the current authenticated session.
 */
export const LogoutRequest = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type LogoutRequest = typeof LogoutRequest.Type

/**
 * An HTTP header to set when making requests to the MCP server.
 */
export const HttpHeader = S.Struct({
  name: S.String,
  value: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type HttpHeader = typeof HttpHeader.Type

/**
 * HTTP transport configuration for MCP.
 */
export const McpServerHttp = S.Struct({
  name: S.String,
  url: S.String,
  headers: S.Array(HttpHeader),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type McpServerHttp = typeof McpServerHttp.Type

/**
 * SSE transport configuration for MCP.
 */
export const McpServerSse = S.Struct({
  name: S.String,
  url: S.String,
  headers: S.Array(HttpHeader),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type McpServerSse = typeof McpServerSse.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * ACP transport configuration for MCP.
 *
 * The MCP server is provided by an ACP component and communicates over the ACP channel
 * using `mcp/connect`, `mcp/message`, and `mcp/disconnect`.
 */
export const McpServerAcp = S.Struct({
  name: S.String,
  serverId: McpServerAcpId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type McpServerAcp = typeof McpServerAcp.Type

/**
 * Stdio transport configuration for MCP.
 */
export const McpServerStdio = S.Struct({
  name: S.String,
  command: S.String,
  args: S.Array(S.String),
  env: S.Array(EnvVariable),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type McpServerStdio = typeof McpServerStdio.Type

/**
 * Configuration for connecting to an MCP (Model Context Protocol) server.
 *
 * MCP servers provide tools and context that the agent can use when
 * processing prompts.
 *
 * See protocol docs: [MCP Servers](https://agentclientprotocol.com/protocol/session-setup#mcp-servers)
 */
export const McpServer = S.Union(
  S.extend(
    McpServerHttp,
    S.Struct({
      type: S.Literal('http'),
    }),
  ),
  S.extend(
    McpServerSse,
    S.Struct({
      type: S.Literal('sse'),
    }),
  ),
  S.extend(
    McpServerAcp,
    S.Struct({
      type: S.Literal('acp'),
    }),
  ),
  McpServerStdio,
)
export type McpServer = typeof McpServer.Type

/**
 * Request parameters for creating a new session.
 *
 * See protocol docs: [Creating a Session](https://agentclientprotocol.com/protocol/session-setup#creating-a-session)
 */
export const NewSessionRequest = S.Struct({
  cwd: S.String,
  additionalDirectories: S.optional(S.Array(S.String)),
  mcpServers: S.Array(McpServer),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NewSessionRequest = typeof NewSessionRequest.Type

/**
 * Request parameters for loading an existing session.
 *
 * Only available if the Agent supports the `loadSession` capability.
 *
 * See protocol docs: [Loading Sessions](https://agentclientprotocol.com/protocol/session-setup#loading-sessions)
 */
export const LoadSessionRequest = S.Struct({
  mcpServers: S.Array(McpServer),
  cwd: S.String,
  additionalDirectories: S.optional(S.Array(S.String)),
  sessionId: SessionId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type LoadSessionRequest = typeof LoadSessionRequest.Type

/**
 * Request parameters for listing existing sessions.
 *
 * Only available if the Agent supports the `sessionCapabilities.list` capability.
 */
export const ListSessionsRequest = S.Struct({
  cwd: S.optional(S.NullOr(S.String)),
  cursor: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ListSessionsRequest = typeof ListSessionsRequest.Type

/**
 * Request parameters for deleting an existing session from `session/list`.
 *
 * Only available if the Agent supports the `sessionCapabilities.delete` capability.
 */
export const DeleteSessionRequest = S.Struct({
  sessionId: SessionId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DeleteSessionRequest = typeof DeleteSessionRequest.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Request parameters for forking an existing session.
 *
 * Creates a new session based on the context of an existing one, allowing
 * operations like generating summaries without affecting the original session's history.
 *
 * Only available if the Agent supports the `session.fork` capability.
 */
export const ForkSessionRequest = S.Struct({
  sessionId: SessionId,
  cwd: S.String,
  additionalDirectories: S.optional(S.Array(S.String)),
  mcpServers: S.optional(S.Array(McpServer)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ForkSessionRequest = typeof ForkSessionRequest.Type

/**
 * Request parameters for resuming an existing session.
 *
 * Resumes an existing session without returning previous messages (unlike `session/load`).
 * This is useful for agents that can resume sessions but don't implement full session loading.
 *
 * Only available if the Agent supports the `sessionCapabilities.resume` capability.
 */
export const ResumeSessionRequest = S.Struct({
  sessionId: SessionId,
  cwd: S.String,
  additionalDirectories: S.optional(S.Array(S.String)),
  mcpServers: S.optional(S.Array(McpServer)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ResumeSessionRequest = typeof ResumeSessionRequest.Type

/**
 * Request parameters for closing an active session.
 *
 * If supported, the agent **must** cancel any ongoing work related to the session
 * (treat it as if `session/cancel` was called) and then free up any resources
 * associated with the session.
 *
 * Only available if the Agent supports the `sessionCapabilities.close` capability.
 */
export const CloseSessionRequest = S.Struct({
  sessionId: SessionId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type CloseSessionRequest = typeof CloseSessionRequest.Type

/**
 * Request parameters for setting a session mode.
 */
export const SetSessionModeRequest = S.Struct({
  sessionId: SessionId,
  modeId: SessionModeId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SetSessionModeRequest = typeof SetSessionModeRequest.Type

/**
 * Request parameters for setting a session configuration option.
 */
export const SetSessionConfigOptionRequest = S.Union(
  S.Struct({
    value: S.Boolean,
    type: S.Literal('boolean'),
  }),
  S.Struct({
    value: SessionConfigValueId,
  }),
)
export type SetSessionConfigOptionRequest = typeof SetSessionConfigOptionRequest.Type

/**
 * Request parameters for sending a user prompt to the agent.
 *
 * Contains the user's message and any additional context.
 *
 * See protocol docs: [User Message](https://agentclientprotocol.com/protocol/prompt-turn#1-user-message)
 */
export const PromptRequest = S.Struct({
  sessionId: SessionId,
  prompt: S.Array(ContentBlock),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type PromptRequest = typeof PromptRequest.Type

/**
 * A workspace folder.
 */
export const WorkspaceFolder = S.Struct({
  uri: S.String,
  name: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type WorkspaceFolder = typeof WorkspaceFolder.Type

/**
 * Repository metadata for an NES session.
 */
export const NesRepository = S.Struct({
  name: S.String,
  owner: S.String,
  remoteUrl: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesRepository = typeof NesRepository.Type

/**
 * Request to start an NES session.
 */
export const StartNesRequest = S.Struct({
  workspaceUri: S.optional(S.NullOr(S.String)),
  workspaceFolders: S.optional(S.NullOr(S.Array(WorkspaceFolder))),
  repository: S.optional(S.NullOr(NesRepository)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type StartNesRequest = typeof StartNesRequest.Type

/**
 * What triggered the suggestion request.
 */
export const NesTriggerKind = S.Literal('automatic', 'diagnostic', 'manual')
export type NesTriggerKind = typeof NesTriggerKind.Type

/**
 * A recently accessed file.
 */
export const NesRecentFile = S.Struct({
  uri: S.String,
  languageId: S.String,
  text: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesRecentFile = typeof NesRecentFile.Type

/**
 * A code excerpt from a file.
 */
export const NesExcerpt = S.Struct({
  startLine: S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)),
  endLine: S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)),
  text: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesExcerpt = typeof NesExcerpt.Type

/**
 * A related code snippet from a file.
 */
export const NesRelatedSnippet = S.Struct({
  uri: S.String,
  excerpts: S.Array(NesExcerpt),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesRelatedSnippet = typeof NesRelatedSnippet.Type

/**
 * An entry in the edit history.
 */
export const NesEditHistoryEntry = S.Struct({
  uri: S.String,
  diff: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesEditHistoryEntry = typeof NesEditHistoryEntry.Type

/**
 * A user action (typing, cursor movement, etc.).
 */
export const NesUserAction = S.Struct({
  action: S.String,
  uri: S.String,
  position: Position,
  timestampMs: S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesUserAction = typeof NesUserAction.Type

/**
 * An open file in the editor.
 */
export const NesOpenFile = S.Struct({
  uri: S.String,
  languageId: S.String,
  visibleRange: S.optional(S.NullOr(Range)),
  lastFocusedMs: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesOpenFile = typeof NesOpenFile.Type

/**
 * Severity of a diagnostic.
 */
export const NesDiagnosticSeverity = S.Literal('error', 'warning', 'information', 'hint')
export type NesDiagnosticSeverity = typeof NesDiagnosticSeverity.Type

/**
 * A diagnostic (error, warning, etc.).
 */
export const NesDiagnostic = S.Struct({
  uri: S.String,
  range: Range,
  severity: NesDiagnosticSeverity,
  message: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesDiagnostic = typeof NesDiagnostic.Type

/**
 * Context attached to a suggestion request.
 */
export const NesSuggestContext = S.Struct({
  recentFiles: S.optional(S.NullOr(S.Array(NesRecentFile))),
  relatedSnippets: S.optional(S.NullOr(S.Array(NesRelatedSnippet))),
  editHistory: S.optional(S.NullOr(S.Array(NesEditHistoryEntry))),
  userActions: S.optional(S.NullOr(S.Array(NesUserAction))),
  openFiles: S.optional(S.NullOr(S.Array(NesOpenFile))),
  diagnostics: S.optional(S.NullOr(S.Array(NesDiagnostic))),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type NesSuggestContext = typeof NesSuggestContext.Type

/**
 * Request for a code suggestion.
 */
export const SuggestNesRequest = S.Struct({
  sessionId: SessionId,
  uri: S.String,
  version: S.Int,
  position: Position,
  selection: S.optional(S.NullOr(Range)),
  triggerKind: NesTriggerKind,
  context: S.optional(S.NullOr(NesSuggestContext)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SuggestNesRequest = typeof SuggestNesRequest.Type

/**
 * Request to close an NES session.
 *
 * The agent **must** cancel any ongoing work related to the NES session
 * and then free up any resources associated with the session.
 */
export const CloseNesRequest = S.Struct({
  sessionId: SessionId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type CloseNesRequest = typeof CloseNesRequest.Type

/**
 * A JSON-RPC request object.
 */
export const ClientRequest = S.Struct({
  id: RequestId,
  method: S.String,
  params: S.optional(
    S.NullOr(
      S.Union(
        InitializeRequest,
        AuthenticateRequest,
        ListProvidersRequest,
        SetProviderRequest,
        DisableProviderRequest,
        LogoutRequest,
        NewSessionRequest,
        LoadSessionRequest,
        ListSessionsRequest,
        DeleteSessionRequest,
        ForkSessionRequest,
        ResumeSessionRequest,
        CloseSessionRequest,
        SetSessionModeRequest,
        SetSessionConfigOptionRequest,
        PromptRequest,
        StartNesRequest,
        SuggestNesRequest,
        CloseNesRequest,
        MessageMcpRequest,
        ExtRequest,
      ),
    ),
  ),
})
export type ClientRequest = typeof ClientRequest.Type

/**
 * Response to `fs/write_text_file`
 */
export const WriteTextFileResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type WriteTextFileResponse = typeof WriteTextFileResponse.Type

/**
 * Response containing the contents of a text file.
 */
export const ReadTextFileResponse = S.Struct({
  content: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ReadTextFileResponse = typeof ReadTextFileResponse.Type

/**
 * The user selected one of the provided options.
 */
export const SelectedPermissionOutcome = S.Struct({
  optionId: PermissionOptionId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type SelectedPermissionOutcome = typeof SelectedPermissionOutcome.Type

/**
 * The outcome of a permission request.
 */
export const RequestPermissionOutcome = S.Union(
  S.Struct({
    outcome: S.Literal('cancelled'),
  }),
  S.extend(
    SelectedPermissionOutcome,
    S.Struct({
      outcome: S.Literal('selected'),
    }),
  ),
)
export type RequestPermissionOutcome = typeof RequestPermissionOutcome.Type

/**
 * Response to a permission request.
 */
export const RequestPermissionResponse = S.Struct({
  outcome: RequestPermissionOutcome,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type RequestPermissionResponse = typeof RequestPermissionResponse.Type

/**
 * Response containing the ID of the created terminal.
 */
export const CreateTerminalResponse = S.Struct({
  terminalId: TerminalId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type CreateTerminalResponse = typeof CreateTerminalResponse.Type

/**
 * Exit status of a terminal command.
 */
export const TerminalExitStatus = S.Struct({
  exitCode: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  signal: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type TerminalExitStatus = typeof TerminalExitStatus.Type

/**
 * Response containing the terminal output and exit status.
 */
export const TerminalOutputResponse = S.Struct({
  output: S.String,
  truncated: S.Boolean,
  exitStatus: S.optional(S.NullOr(TerminalExitStatus)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type TerminalOutputResponse = typeof TerminalOutputResponse.Type

/**
 * Response to terminal/release method
 */
export const ReleaseTerminalResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ReleaseTerminalResponse = typeof ReleaseTerminalResponse.Type

/**
 * Response containing the exit status of a terminal command.
 */
export const WaitForTerminalExitResponse = S.Struct({
  exitCode: S.optional(S.NullOr(S.Int.pipe(S.between(0, Number.MAX_SAFE_INTEGER)))),
  signal: S.optional(S.NullOr(S.String)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type WaitForTerminalExitResponse = typeof WaitForTerminalExitResponse.Type

/**
 * Response to `terminal/kill` method
 */
export const KillTerminalResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type KillTerminalResponse = typeof KillTerminalResponse.Type

/**
 * Allowed wire representations for [`ElicitationContentValue`].
 */
export const ElicitationContentValue = S.Union(S.String, S.Int, S.JsonNumber, S.Boolean, S.Array(S.String))
export type ElicitationContentValue = typeof ElicitationContentValue.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * The user accepted the elicitation and provided content.
 */
export const ElicitationAcceptAction = S.Struct({
  content: S.optional(S.NullOr(S.Record({ key: JsonObjectKey, value: ElicitationContentValue }))),
})
export type ElicitationAcceptAction = typeof ElicitationAcceptAction.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Response from the client to an elicitation request.
 */
export const CreateElicitationResponse = S.Union(
  S.extend(
    ElicitationAcceptAction,
    S.Struct({
      action: S.Literal('accept'),
    }),
  ),
  S.Struct({
    action: S.Literal('decline'),
  }),
  S.Struct({
    action: S.Literal('cancel'),
  }),
)
export type CreateElicitationResponse = typeof CreateElicitationResponse.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Response to `mcp/connect`.
 */
export const ConnectMcpResponse = S.Struct({
  connectionId: McpConnectionId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type ConnectMcpResponse = typeof ConnectMcpResponse.Type

/**
 * **UNSTABLE**
 *
 * This capability is not part of the spec yet, and may be removed or changed at any point.
 *
 * Response to `mcp/disconnect`.
 */
export const DisconnectMcpResponse = S.Struct({
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DisconnectMcpResponse = typeof DisconnectMcpResponse.Type

/**
 * A JSON-RPC response object.
 */
export const ClientResponse = S.Union(
  S.Struct({
    id: RequestId,
    result: S.Union(
      WriteTextFileResponse,
      ReadTextFileResponse,
      RequestPermissionResponse,
      CreateTerminalResponse,
      TerminalOutputResponse,
      ReleaseTerminalResponse,
      WaitForTerminalExitResponse,
      KillTerminalResponse,
      CreateElicitationResponse,
      ConnectMcpResponse,
      DisconnectMcpResponse,
      MessageMcpResponse,
      ExtResponse,
    ),
  }),
  S.Struct({
    id: RequestId,
    error: Error,
  }),
)
export type ClientResponse = typeof ClientResponse.Type

/**
 * Notification to cancel ongoing operations for a session.
 *
 * See protocol docs: [Cancellation](https://agentclientprotocol.com/protocol/prompt-turn#cancellation)
 */
export const CancelNotification = S.Struct({
  sessionId: SessionId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type CancelNotification = typeof CancelNotification.Type

/**
 * Notification sent when a file is opened in the editor.
 */
export const DidOpenDocumentNotification = S.Struct({
  sessionId: SessionId,
  uri: S.String,
  languageId: S.String,
  version: S.Int,
  text: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DidOpenDocumentNotification = typeof DidOpenDocumentNotification.Type

/**
 * A content change event for a document.
 *
 * When `range` is `None`, `text` is the full content of the document.
 * When `range` is `Some`, `text` replaces the given range.
 */
export const TextDocumentContentChangeEvent = S.Struct({
  range: S.optional(S.NullOr(Range)),
  text: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type TextDocumentContentChangeEvent = typeof TextDocumentContentChangeEvent.Type

/**
 * Notification sent when a file is edited.
 */
export const DidChangeDocumentNotification = S.Struct({
  sessionId: SessionId,
  uri: S.String,
  version: S.Int,
  contentChanges: S.Array(TextDocumentContentChangeEvent),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DidChangeDocumentNotification = typeof DidChangeDocumentNotification.Type

/**
 * Notification sent when a file is closed.
 */
export const DidCloseDocumentNotification = S.Struct({
  sessionId: SessionId,
  uri: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DidCloseDocumentNotification = typeof DidCloseDocumentNotification.Type

/**
 * Notification sent when a file is saved.
 */
export const DidSaveDocumentNotification = S.Struct({
  sessionId: SessionId,
  uri: S.String,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DidSaveDocumentNotification = typeof DidSaveDocumentNotification.Type

/**
 * Notification sent when a file becomes the active editor tab.
 */
export const DidFocusDocumentNotification = S.Struct({
  sessionId: SessionId,
  uri: S.String,
  version: S.Int,
  position: Position,
  visibleRange: Range,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type DidFocusDocumentNotification = typeof DidFocusDocumentNotification.Type

/**
 * Notification sent when a suggestion is accepted.
 */
export const AcceptNesNotification = S.Struct({
  sessionId: SessionId,
  id: NesSuggestionId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type AcceptNesNotification = typeof AcceptNesNotification.Type

/**
 * The reason a suggestion was rejected.
 */
export const NesRejectReason = S.Literal('rejected', 'ignored', 'replaced', 'cancelled')
export type NesRejectReason = typeof NesRejectReason.Type

/**
 * Notification sent when a suggestion is rejected.
 */
export const RejectNesNotification = S.Struct({
  sessionId: SessionId,
  id: NesSuggestionId,
  reason: S.optional(S.NullOr(NesRejectReason)),
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type RejectNesNotification = typeof RejectNesNotification.Type

/**
 * A JSON-RPC notification object.
 */
export const ClientNotification = S.Struct({
  method: S.String,
  params: S.optional(
    S.NullOr(
      S.Union(
        CancelNotification,
        DidOpenDocumentNotification,
        DidChangeDocumentNotification,
        DidCloseDocumentNotification,
        DidSaveDocumentNotification,
        DidFocusDocumentNotification,
        AcceptNesNotification,
        RejectNesNotification,
        MessageMcpNotification,
        ExtNotification,
      ),
    ),
  ),
})
export type ClientNotification = typeof ClientNotification.Type

/**
 * Notification to cancel an ongoing request.
 *
 * See protocol docs: [Cancellation](https://agentclientprotocol.com/protocol/cancellation)
 */
export const CancelRequestNotification = S.Struct({
  requestId: RequestId,
  _meta: S.optional(S.NullOr(JsonObject)),
})
export type CancelRequestNotification = typeof CancelRequestNotification.Type

export const AgentRequestMessage = S.Union(
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('fs/write_text_file'),
    params: WriteTextFileRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('fs/read_text_file'),
    params: ReadTextFileRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/request_permission'),
    params: RequestPermissionRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('terminal/create'),
    params: CreateTerminalRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('terminal/output'),
    params: TerminalOutputRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('terminal/release'),
    params: ReleaseTerminalRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('terminal/wait_for_exit'),
    params: WaitForTerminalExitRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('terminal/kill'),
    params: KillTerminalRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('elicitation/create'),
    params: CreateElicitationRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('mcp/connect'),
    params: ConnectMcpRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('mcp/message'),
    params: MessageMcpRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('mcp/disconnect'),
    params: DisconnectMcpRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.String,
    params: ExtRequest,
  }),
)
export type AgentRequestMessage = typeof AgentRequestMessage.Type

export const ClientRequestMessage = S.Union(
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('initialize'),
    params: InitializeRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('authenticate'),
    params: AuthenticateRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('providers/list'),
    params: ListProvidersRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('providers/set'),
    params: SetProviderRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('providers/disable'),
    params: DisableProviderRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('logout'),
    params: LogoutRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/new'),
    params: NewSessionRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/load'),
    params: LoadSessionRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/list'),
    params: ListSessionsRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/delete'),
    params: DeleteSessionRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/fork'),
    params: ForkSessionRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/resume'),
    params: ResumeSessionRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/close'),
    params: CloseSessionRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/set_mode'),
    params: SetSessionModeRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/set_config_option'),
    params: SetSessionConfigOptionRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('session/prompt'),
    params: PromptRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('nes/start'),
    params: StartNesRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('nes/suggest'),
    params: SuggestNesRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('nes/close'),
    params: CloseNesRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.Literal('mcp/message'),
    params: MessageMcpRequest,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    id: RequestId,
    method: S.String,
    params: ExtRequest,
  }),
)
export type ClientRequestMessage = typeof ClientRequestMessage.Type

export const AgentNotificationMessage = S.Union(
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('session/update'),
    params: SessionNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('elicitation/complete'),
    params: CompleteElicitationNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('mcp/message'),
    params: MessageMcpNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.String,
    params: ExtNotification,
  }),
)
export type AgentNotificationMessage = typeof AgentNotificationMessage.Type

export const ClientNotificationMessage = S.Union(
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('session/cancel'),
    params: CancelNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('document/didOpen'),
    params: DidOpenDocumentNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('document/didChange'),
    params: DidChangeDocumentNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('document/didClose'),
    params: DidCloseDocumentNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('document/didSave'),
    params: DidSaveDocumentNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('document/didFocus'),
    params: DidFocusDocumentNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('nes/accept'),
    params: AcceptNesNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('nes/reject'),
    params: RejectNesNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.Literal('mcp/message'),
    params: MessageMcpNotification,
  }),
  S.Struct({
    jsonrpc: S.Literal('2.0'),
    method: S.String,
    params: ExtNotification,
  }),
)
export type ClientNotificationMessage = typeof ClientNotificationMessage.Type
