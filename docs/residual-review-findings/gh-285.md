# Residual Review Findings

Branch: `gh-285`. Review run: `/tmp/compound-engineering-0/ce-code-review/20260828-022658-4ee3f90b`.

Applied: write-guard list parse now treats quoted, unquoted, comment-separated, and fragment list items as exclude candidates (probe: own-org allow, lodash/react/comment-mixed refuse, `packages/*` ignored).

## Settled — not applied

- Dual stryker source (`catalog:stryker` registry vs `workspace:^` for daemon-spec/hex-schema/omp): user-directed registry consumption to break the workspace SCC.
- cli oxlint-plugin entries not peer-promoted: user rejected `peerDependencies`; plugins load via `@systemfsoftware/all`.

## Unapplied

- Hook `decide()` has no unit test. Evaluator surface; OP12 default-deny. Gate if added: `deno test` on constructed payloads, not a mutation run (REPO-D3).
- Plan U1 step 5 still says peer-promote. Code removes the edges. CONST-W3: the plan text is stale relative to the tree.
