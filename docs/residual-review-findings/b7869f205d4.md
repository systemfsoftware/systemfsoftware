# Residual review findings — rightzone-node-fork @ b7869f205d4

Ten-persona ce-code-review over the branch diff (base `dae6f86f378`).
17 findings applied and verified in `b7869f205d4` (semantics, surface, tests).
Everything below survived triage as advisory — recorded, deliberately not applied.

## Design calls (need a human or an upstream capability)

1. **msbAgentEndpoint has no producer** (`src/fleet/handle.ts:451`). The byId
   reachability gate for msb handles is dead code until the msb runtime
   publishes its agent endpoint into handle minting. Requires plumbing an
   endpoint source through `layerMsb`; not invented here because the endpoint's
   lifecycle (per-runtime vs per-container) is an msb design question.
2. **Parity enumerates member names only** (`scripts/parity-enumerate.mjs:128`).
   A signature-level change inside a frozen testcontainers member would not trip
   the gate. The frozen surface is the W8 record (docstring now says so);
   signature hashing would need the testcontainers tarball re-vendored — decided
   against because `repos/` governance makes that a new subtree decision.

## Invariant-backed observations (verified, no action)

3. **Fleet live rows report `'running'` without re-probe**
   (`src/fleet/fleet.ts:56`). The in-process registry only holds running
   containers (teardown unregisters — fixed in the seam-consolidation commit);
   out-of-band removal surfaces as `'missing'` via the ledger path. A per-row
   probe would double the listing's backend calls for a state the registry
   already owns.
4. **Reap kill pass routes by CLI regardless of `entry.backend`**
   (`src/fleet/reap.ts:229`). Single-run ledgers never mix backends today
   (launch binds one backend per run); the guarantee is enforced at launch, not
   re-checked at reap. If mixed-run ledgers ever become real, route kills
   through the entry's backend tag.

## Bookkeeping (correctness-neutral)

5. `src/lifecycle/hygiene/ledger.ts` keeps its own tmp+rename beside
   `src/internal/atomic-write.ts` — both correct; consolidation is cosmetic.
6. `scripts/parity-enumerate.mjs` is one large file whose bulk is the committed
   MAPPING data (the record). Splitting would touch only presentation.
7. Standards' "missing changeset for effect-workflow" was stale: the
   LOCAL_PURE_SOURCES seal edit ships under committed
   `.changeset/make-boundary-regime.md` (effect-workflow: major).

## Honored session settlements

- Suffix taxonomy dismantled; `*.workflow.ts` / `*.workflow.property.test.ts` /
  `*.integration.test.ts` are the only sanctioned suffixes (leaf AGENTS.md
  RS-BOUNDARY). TP4/TP5 admitPlainStems options are the user-settled mechanism.
- No agent mutation runs (REPO-D3). Mutation workflow's merged report owns
  scores.

## Follow-ups landed after this record

- `6b493246ca4`: the live CI lane runs surfaced a second-order break — the
  contract lanes npm-install the packed CLI tarballs inside their container,
  where `effect` resolves to the registry's current rc (the lockfile pins
  rc.108) and Effect CLI rc.109+ treats a defaultless flag as required. The
  optional boolean flags in both CLIs now carry explicit `false` defaults;
  stryker 24/24 and attw 10/10 re-verified in-container.
- The parity gate no longer requires dprint to run: `--check` compares
  cell-normalized table content when no formatter resolves (CI runners have
  neither dprint nor nix), while `--write` still refuses to emit unformatted
  bytes. The frozen-surface gate compares the imported module as data.

Verification state at time of writing: typecheck 0 errors, 316 tests green
(31 files), lint clean, build + parity:check green, attw clean,
`pnpm check:local` EXIT=0, stryker lane 24/24, attw lane 10/10.
