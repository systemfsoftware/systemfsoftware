{
  "name": "systemfsoftware",
  "private": true,
  "description": "System F Software — Effect-TS libraries and the oxlint plugin that enforces the constitution.",
  "type": "module",
  "scripts": {
    "build": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} build",
    "typecheck": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} --continue typecheck",
    "test": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} test --filter=!@systemfsoftware/vitest-config",
    "lint": "pnpm format:check && turbo --concurrency=${TURBO_CONCURRENCY:-50%} lint ${AGENT:+-- --format=unix}",
    "lint:ci": "pnpm format:check && turbo --concurrency=${TURBO_CONCURRENCY:-50%} lint -- --format=github",
    "api:check": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} api:check",
    "format": "dprint fmt",
    "format:check": "dprint check",
    "precommit": "pnpm exec lint-staged --no-revert",
    "pre-push": "concurrently --group \"pnpm lint\" \"pnpm typecheck\" \"pnpm attw\" \"pnpm check:exports\" \"pnpm api:check\" \"CI=true pnpm test\"",
    "check": "pnpm install --frozen-lockfile && concurrently --group --no-bail \"pnpm lint\" \"pnpm typecheck\" \"pnpm attw\" \"pnpm check:exports\" \"pnpm test\" \"pnpm api:check\"",
    "check:ci": "concurrently --group --no-bail \"pnpm lint:ci\" \"pnpm typecheck\" \"pnpm attw\" \"pnpm check:exports\" \"pnpm api:check\" \"CI=true pnpm test\"",
    "release": "node scripts/release.mjs",
    "prepare": "husky",
    "release:dry": "node scripts/release.mjs --dry-run",
    "bump:minor": "node scripts/bump-all-minor.mjs",
    "publish:check": "node scripts/validate-publish-config.mjs",
    "attw": "turbo --concurrency=${TURBO_CONCURRENCY:-50%} attw",
    "check:exports": "node scripts/check-exports.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@commitlint/config-pnpm-scopes": "^21.2.0",
    "@commitlint/types": "^21.2.0",
    "@effect/language-service": "^0.87.0",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "catalog:",
    "@semantic-release/github": "^11.0.6",
    "@semantic-release/release-notes-generator": "^14.0.3",
    "@types/chai": "^5",
    "@types/chai-as-promised": "^7",
    "@types/chai-jest-snapshot": "^1",
    "@types/mocha": "^10",
    "@types/sinon": "^21",
    "@types/sinon-chai": "^3",
    "@types/source-map-support": "^0.5",
    "c8": "^11",
    "chai": "^5",
    "chai-as-promised": "^7",
    "chai-jest-snapshot": "^2",
    "concurrently": "^9.2.1",
    "conventional-changelog-conventionalcommits": "^9.1.0",
    "dprint": "^0.55.2",
    "estree-walker": "^3",
    "husky": "^9.1.7",
    "lint-staged": "^17.2.0",
    "mocha": "^11",
    "oxlint": "catalog:oxlint",
    "oxlint-tsgolint": "catalog:oxlint",
    "semantic-release": "^24.2.9",
    "sinon": "^21",
    "sinon-chai": "^3",
    "source-map-support": "^0.5",
    "turbo": "^2.10.5",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@11.9.0"
}
