{
  "ignore": [
    "repos/**",
    ".worktrees/**"
  ],
  "stale_ref_allowlist": {
    "reports/stryker-incremental.json": "Per-package path, deliberately written package-relative in the root mutation-evidence rule. It exists as packages/<pkg>/reports/stryker-incremental.json only after a mutation run, so no repo-root file can ever resolve it.",
    "src/schema-laws.test.ts": "A file in the DOWNSTREAM CONSUMER's project, not this repo. effect-schema-vite generates it in the consuming app via its transform hook; the basename is load-bearing and documented for that reason.",
    "src/index.ts": "Generic per-plugin barrel referenced collectively in omp/AGENTS.md prose, not one file. Each omp plugin has its own; no single path resolves.",
    "omp/CLAUDE.md": "A hypothetical in a troubleshooting table describing what to do IF someone adds this file. Its absence is the correct state the row defends."
  }
}
