import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  index: "Overview",
  compile: "Compiler (ttsc)",
  execute: "Execute (ttsx)",
  cache: "Build cache",
  flags: "CLI flags",
};
export default meta;
