# TTSC

The compiler and the runtime, one toolchain, one `tsconfig.json`.

```bash
npx ttsc           # build · check · watch · fix · format
npx ttsx file.ts   # run TypeScript with a real type-check
```

## In this chapter

- [Compile](/docs/ttsc/compile): `ttsc` build / check / watch / fix / format. Day-to-day commands.
- [Execute](/docs/ttsc/execute): `ttsx`. The typed replacement for `tsx` and `ts-node`.
- [CLI flags](/docs/ttsc/flags): the full flag reference.

Bundler setups (`@ttsc/unplugin`, `@ttsc/metro`) live in [Setup → Unplugin](/docs/setup/unplugin) and [Setup → Metro](/docs/setup/metro).

To show TypeScript errors and supported plugin diagnostics in VS Code, install the extension as part of [Setup → VS Code](/docs/setup/vscode). For lint and format, see [Lint & Prettier](/docs/lint). For the utility plugins shipped in this repo and the wider ecosystem (typia, nestia, …), see [Plugin Ecosystem](/docs/plugins).

## How they fit together

Every `ttsc` build is one pass over your project:

1. Read `tsconfig.json`.
2. Type-check the project.
3. Run every configured plugin, in order.
4. Emit JS + `.d.ts` (or skip with `--noEmit`).

`ttsx` does steps 1–3, then runs the result instead of emitting. [`@ttsc/unplugin`](/docs/setup/unplugin) runs the same plugin pass inside a bundler. The VS Code extension shows TypeScript-Go editor features plus supported plugin diagnostics and actions from the same project setup. It does not emit or rewrite JavaScript.
