# Compiler bundle output

This directory is reserved for the future Web Worker / WASM compiler bundle. Currently the playground compiles on the main thread; see `src/compiler/index.ts` for the implementation.
