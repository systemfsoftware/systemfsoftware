# `@ttsc/website`

The `ttsc.dev` site. Built with Next.js 15 (App Router, static export) + Nextra 4 + Tailwind v4.

## Three sections

- **Docs**: Guide documents for consumers, plugin authors, lint users, and editor users (`src/content/docs/`).
- **Blog**: MDX articles under `src/content/blog/`, rendered by the `src/app/blog/` routes; `build/blog.cjs` generates `/blog/rss.xml` at build time, and the root layout advertises it for RSS reader auto-discovery.
- **Playground**: Monaco-based in-browser editor backed by the full Go compiler as wasm, with the banner, paths, strip, lint, and typia plugins linked in.

## Commands

```bash
pnpm install
pnpm dev       # compiler worker rebuild + next dev
pnpm build     # static export into ./out
pnpm deploy    # publish ./out to gh-pages
```

## Layout

Abridged; only the load-bearing entries are shown.

```
website/
├── src/
│   ├── app/                            # Next.js App Router routes
│   │   ├── layout.jsx                  # Nextra navbar/footer/sidebar shell
│   │   ├── global.css                  # Tailwind + Nextra theme + landing tweaks
│   │   ├── [[...mdxPath]]/             # Catch-all MDX pages (docs, landing)
│   │   ├── blog/                       # Blog index, per-post, and tag routes
│   │   └── playground/                 # Interactive playground route
│   ├── content/
│   │   ├── _meta.ts                    # Top-level navigation (docs / blog / playground)
│   │   ├── index.mdx                   # Landing page
│   │   ├── docs/                       # Guide documents
│   │   └── blog/                       # Blog articles (MDX)
│   ├── components/
│   │   ├── benchmark/                  # Benchmark dashboard + charts
│   │   ├── graph/                      # 3D graph viewer
│   │   └── playground/                 # Wrapper around @ttsc/playground's PlaygroundShell
│   ├── compiler/                       # Worker boundary (tgrid)
│   │   ├── index.ts                    # Worker entry bundled by build/compiler.cjs
│   │   ├── PlaygroundExamples.ts       # Default playground example sources
│   │   └── typia-types.json            # Generated by build/typia-types.cjs (gitignored)
│   ├── movies/landing/                 # Landing page sections (hero, lint, plugins, sponsors, …)
│   └── structures/                     # Shared TS interfaces (benchmark, graph viewer)
├── compiler/                           # Go module producing the playground wasm (cmd/playground)
├── public/
│   ├── compiler/                       # Output of build/compiler.cjs (playground.wasm + worker)
│   ├── favicon/                        # Favicon set
│   └── og.jpg
└── build/
    ├── compiler.cjs                    # Builds playground.wasm + bundles the worker
    ├── blog.cjs                        # Blog RSS feed
    ├── typia-types.cjs                 # Packs typia .d.ts sources for Monaco
    └── deploy.cjs                      # gh-pages deploy
```

## Playground compiler

The playground keeps typia's worker-boundary architecture (tgrid `WorkerConnector` on the main thread, `WorkerServer` in the worker), with the backend supplied by [`@ttsc/playground`](https://github.com/samchon/ttsc/tree/master/packages/playground).

`build/compiler.cjs` produces the three artifacts the playground loads at runtime:

1. **`public/compiler/playground.wasm`**: the website's consumer wasm, built from the Go module in `website/compiler/` (`cmd/playground`). It links the real Go engines for `@ttsc/banner`, `@ttsc/paths`, `@ttsc/strip`, `@ttsc/lint`, and typia.
2. **`public/compiler/wasm_exec.js`**: Go's bootstrap shim, copied from `@ttsc/wasm`.
3. **`public/compiler/index.js`**: the rspack-bundled Web Worker from `src/compiler/index.ts`, which wires the site's URL conventions and the typia source pack into `createWorkerCompiler` from `@ttsc/playground`.

The Typia source-pack mount has no deadline over the response or JSON body; the network decides how long they take. Concurrent loads share one request; a caller abort or other rejection evicts that request so the next compiler request can fetch a fresh copy without starting another WASM runtime.

`build/typia-types.cjs` packs typia's declaration sources into `src/compiler/typia-types.json` so Monaco's `addExtraLib` sees the same type surface the compiler does.
