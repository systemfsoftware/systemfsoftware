# Simulation is selected by the Vite mode, never here. `vite.config.ts` sets the
# flag from the mode before Vite reads any env file, so a value written here is
# overwritten rather than honored. Use `--mode contract` to build simulated.
VITE_API_HOST=http://127.0.0.1:37001
