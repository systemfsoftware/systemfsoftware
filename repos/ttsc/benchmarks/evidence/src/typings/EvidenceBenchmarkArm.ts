/**
 * Selects the sole treatment variable of a measured benchmark cell.
 *
 * Plain receives only the shared template and instructions, while Evidence
 * additionally receives the plugin archive, overlay, and graph guidance.
 */
export type EvidenceBenchmarkArm = "evidence" | "plain";
