/** Fixture: a config object owned by a module of its own, spread into another module's config. */
import { ButtonComponent } from './lib/button';

export const base = { component: ButtonComponent };
