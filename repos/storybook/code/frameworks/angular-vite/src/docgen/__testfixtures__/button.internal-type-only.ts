/** Fixture: a story-config module whose binding is type-only, not the story file's. */
import type { ButtonComponent } from './button.component';

export const config = { component: ButtonComponent, args: {} };
