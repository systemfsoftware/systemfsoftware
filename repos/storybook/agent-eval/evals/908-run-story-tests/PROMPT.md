Run tests for the Button component stories using the Storybook MCP testing tool.

Use the `test-run` tool and report which stories passed or failed.

Do not change component or story files unless a test failure requires a fix.
