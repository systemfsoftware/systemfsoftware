---
"@systemfsoftware/effect-schema-law": minor
---

Narrow the entry to the names it means to publish. `refutes` and `ruleOfSchemas` move to harnesses; the remaining refutation and weakening surfaces are reached through the `Refutation` and `Weakening` namespaces.
