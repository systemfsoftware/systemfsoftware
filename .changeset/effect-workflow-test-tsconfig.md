---
"@systemfsoftware/oxlint-plugin-effect-workflow": none
---

Build configuration only: the package's tests compile under their own tsconfig so @types/node never enters the production program — nothing shipped changes
