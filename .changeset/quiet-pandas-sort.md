---
"@systemfsoftware/stryker-js-cli": patch
---

The optional boolean flags (--incremental, --force, --dry-run-only, and the other omitted-by-default switches) now default to false when omitted, restoring runs that pass none of them
