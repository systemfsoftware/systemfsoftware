---
"@systemfsoftware/stryker-js-html-reporter": major
---

`makeHtmlReporter` is removed. The reporter is available only as `strykerPlugins`, which binds the port itself; consumers who composed the factory by hand migrate to the plugin bundle.
