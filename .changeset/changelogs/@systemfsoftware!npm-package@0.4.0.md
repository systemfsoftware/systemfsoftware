## 0.4.0

### Minor Changes

- `makePackage`, `createPackage`, `toDirectoryJSON`, `packTree` and the path helpers can now also be called data-last, with the files argument supplied last. Existing calls keep working.
