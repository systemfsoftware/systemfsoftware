## 3.2.0

### Minor Changes

- The config now enables `@systemfsoftware/effect-schema/tagged-error-requires-message`: a `Schema.TaggedError` class must declare a `message` field or a `message` getter. Add one to any class it reports.
