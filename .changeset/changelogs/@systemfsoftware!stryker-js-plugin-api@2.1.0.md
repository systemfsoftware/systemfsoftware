## 2.1.0

### Minor Changes

- Plugins can now declare an `Evaluator`. The host runs every loaded evaluator against the finished mutation report. Listing the plugin module is enough; there is no separate name list.
