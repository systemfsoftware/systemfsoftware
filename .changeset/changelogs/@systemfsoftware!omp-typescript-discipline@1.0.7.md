## 1.0.7

### Patch Changes

- The tsconfig rule now also covers a package's app project when the package is split into project references, allows `node` types in test projects, and no longer interrupts the agent mid-stream.
