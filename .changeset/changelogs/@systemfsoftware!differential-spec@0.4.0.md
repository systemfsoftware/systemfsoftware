## 0.4.0

### Minor Changes

- Requires `@systemfsoftware/vitest` installed under the `@effect/vitest` name (`"@effect/vitest": "npm:@systemfsoftware/vitest"`) in place of the upstream package. A comparison that finds no disparity counts as the test's assertion.
