---
"@systemfsoftware/trace-spec": patch
---

The observation window and its resource are built on the `Resource` and `Handle` kinds, with unchanged behaviour. The resource's `TypeId` is now exported as a value.
