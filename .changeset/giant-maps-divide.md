---
"@systemfsoftware/effect-daemon-conformance": minor
---

`ScenarioCompared` and `ScenarioStalled` are tagged structs instead of classes. Their encoded shapes are unchanged; build values with `.make(...)` instead of `new`.
