---
"@systemfsoftware/stryker-js-html-reporter": none
---

No release is warranted. When a bundler inlines the HTML client bundle it renders from that copy, and otherwise it reads the bundle from the installed package exactly as before. Every exported name, type and behaviour is unchanged.
