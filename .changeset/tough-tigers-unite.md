---
"@systemfsoftware/effect-spec-runtime": minor
---

Requires `@systemfsoftware/vitest` installed under the `@effect/vitest` name (`"@effect/vitest": "npm:@systemfsoftware/vitest"`) in place of the upstream package. `TaskRef` reads the running test's context in property tests as well as in case tests.
