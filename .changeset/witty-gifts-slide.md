---
"@systemfsoftware/stryker-js-mutation-run": major
---

The default set of test-file suffixes the contribution check gates narrowed to the workflow property suffix alone.

A consumer who relied on the previous default to keep policy or kernel property tests covered must now set `requireTestContribution` explicitly for them.
