---
"@systemfsoftware/stryker-plugins": major
---

The ignore decision functions and their reason constants are no longer exported; each package entry now publishes only its plugin bundle. Drive the decisions through the bound ignorer service the plugin layer provides.
