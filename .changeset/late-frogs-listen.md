---
"@systemfsoftware/arethetypeswrong-cli": patch
---

The optional boolean flags (--pack, --from-npm, --quiet, --entrypoints-legacy) now default to false when omitted, restoring runs that pass none of them
