---
"@systemfsoftware/effect-cell-types": minor
---

`Cell.vocabulary` gains a `shell` table naming the shell constructors — `run`, `andThen`, `zip`, `provide` — so tooling can bind the published surface at load instead of restating it.
