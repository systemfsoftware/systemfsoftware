# AGENTS.md — `@systemfsoftware/api-extractor`

Root `AGENTS.md` governs; these are the package-local rules and the gate each one names.

| ID        | Rule                                                                                                                                                                                                                                                                                                                                                 | Gate                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **AE-R1** | `src/` and the manifest's `dependencies`/`peerDependencies` name no `@rushstack/*` or `@microsoft/api-extractor*` package. Effect 4, `@effect/platform-node`, and Node built-ins replaced the legacy Rushstack runtime; upstream doc comments may still name what they replaced. The upstream package may appear only as a `devDependencies` oracle. | `grep -rn -e '^[}ie].*@rushstack/' -e '^[}ie].*@microsoft/api-extractor' -e "import('@rushstack/" -e "import('@microsoft/api-extractor" -e "require('@rushstack/" -e "require('@microsoft/api-extractor" packages/api-extractor/src --include='*.ts'` prints nothing, and `node -e "const p=require('./packages/api-extractor/package.json');const b={...p.dependencies,...p.peerDependencies};if(Object.keys(b).some(k=>k.startsWith('@rushstack/') |
| **AE-R2** | `src/` holds no interpretation edge: `Effect.runSync`, `Effect.runPromise`, `Effect.runFork`, and `Effect.orDie` appear nowhere. `NodeRuntime.runMain` in `src/main.ts` is the package's only run boundary.                                                                                                                                          | `grep -rn -e 'runSync' -e 'runPromise' -e 'runFork' -e 'orDie' packages/api-extractor/src` prints nothing                                                                                                                                                                                                                                                                                                                                            |
| **AE-R3** | The package root exports exactly one namespace: `src/mod.ts` is the single line `export * as Extractor from './Extractor/mod.js'`. Analyzer, collector, generator, and enhancer modules stay internal; the root exports no second binding and no `Promise`-returning entry point.                                                                    | `grep -vc "^export \* as Extractor from './Extractor/mod.js'$" packages/api-extractor/src/mod.ts` prints `0`                                                                                                                                                                                                                                                                                                                                         |
| **AE-R4** | Layers are bound only in `src/main.ts`: `Effect.provide` and `Layer.provide` appear in no other `src/` file. A driver may build its own service `layer` value with `Layer.` combinators (the service's server side), but never binds it; only the CLI entry binds layers into a run.                                                                 | `grep -rn -e 'Layer\.' -e 'Effect\.provide' packages/api-extractor/src --exclude=main.ts --exclude-dir=drivers` prints nothing                                                                                                                                                                                                                                                                                                                       |
| **AE-R5** | No child process is spawned outside `examples/`. `node:child_process`, `exec`, `spawn`, and `fork` appear nowhere in `src/` or the test suites; analysis, collection, generation, and every CLI journey run in-process (the packed-install journey in `examples/` is the one exception).                                                             | `grep -rn -e 'child_process' -e 'execSync' -e 'spawnSync' packages/api-extractor/src packages/api-extractor/tests` prints nothing                                                                                                                                                                                                                                                                                                                    |
| **AE-R6** | Typed tagged errors only: nothing in `src/` throws a built-in `Error`. User-input failures are `ExtractorError` variants; broken internal invariants are the one `InternalInvariantError` defect; compiler and TSDoc diagnostics are `ExtractorMessage` data routed by the message workflows.                                                        | `grep -rEn -e 'throw new Error' -e 'throw new TypeError' -e 'throw new RangeError' packages/api-extractor/src` prints nothing                                                                                                                                                                                                                                                                                                                        |
| **AE-R7** | Analysis runs on the bundled `typescript@5.9.3`, and behavior is pinned to upstream `microsoft/rushstack@bc4cd29cc4984cef267437565bf681582948285e` (npm `@microsoft/api-extractor@7.59.1`). The pin is stated in `README.md` provenance and the bundled compiler version is a regular `dependencies` entry, never the consumer's compiler.           | `grep -q 'bc4cd29' packages/api-extractor/README.md && grep -q '"typescript": "5.9.3"' packages/api-extractor/package.json` exits 0                                                                                                                                                                                                                                                                                                                  |

## Verification

```bash
pnpm --filter @systemfsoftware/api-extractor typecheck
pnpm --filter @systemfsoftware/api-extractor lint
pnpm --filter @systemfsoftware/api-extractor test
pnpm --filter @systemfsoftware/api-extractor build
```

The package's own `api:check` runs in verification mode (`node dist/main.mjs run --quiet`), so drift in `etc/api-extractor.api.md` fails `build`; `api:update` adds `--local` to rewrite it.
