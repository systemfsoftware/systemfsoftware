/**
 * `@systemfsoftware/api-extractor` — standalone, Effect-native API report
 * and declaration rollup engine for TypeScript libraries.
 *
 * This placeholder export establishes the typed package surface for the U1
 * scaffold. U6 introduces `runEffect` and `invoke`.
 */
export const version = '0.1.0'
