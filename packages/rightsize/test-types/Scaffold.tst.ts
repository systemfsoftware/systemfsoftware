import { describe, expect, it } from 'tstyche'

// U1 scaffold placeholder: the shape-law and type-contract suites arrive with
// the units that own them (U2 domain schemas, U3 runtime contracts, …). This
// file keeps `test:types` wired and honest until the first real type test.
describe('the scaffold placeholder', () => {
  it('Should_Typecheck_When_LawSuitesHaveNotLandedYet', () => {
    expect<1>().type.toBe<1>()
  })
})
