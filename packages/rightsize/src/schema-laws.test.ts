// Generated: @systemfsoftware/effect-schema-vite rewrites this file, injecting a
// `ruleOfSchemas` law pair for every exported Effect Schema in this package.
// The generated body carries real import edges to each schema file so vitest's
// related-file walk (and therefore `stryker --related`) reaches them.
//
// U1 scaffold: no schema is exported yet, so the plugin generates
// `// no schemas found\nexport {}` and this suite passes empty (`passWithNoTests`
// in the shared vitest base). The laws arrive with the first exported schema
// in the domain-data unit; keep this file exactly as the plugin's canonical
// law entry — nothing else may own this filename.
export {}
