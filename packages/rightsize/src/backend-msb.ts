/**
 * `@systemfsoftware/rightsize/backend-msb` — the microsandbox backend.
 *
 * U1 scaffold: an intentionally empty entry. `layerMsb` (self-provisioning
 * pinned `msb` runtime, attached-mode CLI driver, network-alias tunnels)
 * arrives in the msb-backend unit and is exported from here.
 *
 * @since 0.1.0
 */
export {}
