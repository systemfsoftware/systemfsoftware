/**
 * `@systemfsoftware/rightsize/modules` — the module presets surface.
 *
 * U1 scaffold: an intentionally empty barrel. The 23 upstream module presets
 * plus the parity-matrix additions arrive as Schema data in the modules unit
 * (KTD11 — data presets, never classes), and this barrel is where they are
 * exported from.
 *
 * @since 0.1.0
 */
export {}
