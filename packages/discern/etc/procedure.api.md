## API Report File for "@systemfsoftware/discern"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import * as AiError from 'effect/unstable/ai/AiError';
import * as Context from 'effect/Context';
import * as Decision from 'effect/unstable/ai/Decision';
import * as DecisionModel from 'effect/unstable/ai/DecisionModel';
import * as Effect from 'effect/Effect';
import * as Schema from 'effect/Schema';

// @public (undocumented)
export type Any = Procedure<string, any, any, any, any, Schema.Constraint>;

// @public
export const CurrentDepth: Context.Reference<number>;

// @public (undocumented)
export class DepthExceededError extends Error {
    constructor(depth: number, limit: number);
    // (undocumented)
    readonly depth: number;
    // (undocumented)
    readonly limit: number;
    // (undocumented)
    readonly name = "DepthExceededError";
    // (undocumented)
    readonly _tag = "DepthExceededError";
}

// @public (undocumented)
export type EffectError<T> = T extends Effect.Effect<any, infer E, any> ? E : never;

// @public (undocumented)
export type EffectRequirements<T> = T extends Effect.Effect<any, any, infer R> ? R : never;

// @public (undocumented)
export type EffectSuccess<T> = T extends Effect.Effect<infer A, any, any> ? A : T;

// @public (undocumented)
export type ErrorOf<C> = C extends Procedure<any, any, any, infer E, any, any> ? E : never;

// @public
export const fromEffect: <const Id extends string, S extends Schema.Constraint, Out, Err, Req>(id: Id, description: string, input: S, run: (input: S['Type']) => Effect.Effect<Out, Err, Req>) => Procedure<Id, S['Type'], Out, Err, Req, S>;

// @public
export const fromRegistry: <const Id extends string, S extends Schema.Constraint, const Members extends ReadonlyArray<Procedure<string, S['Type'], any, any, any, S>>>(options: {
    readonly id: Id;
    readonly description: string;
    readonly examples?: ReadonlyArray<string>;
    readonly registry: Registry<Members, S>;
    readonly routing?: RouteOptions;
}) => Procedure<Id, S['Type'], OutputOf<Members[number]>, ErrorOf<Members[number]> | AiError.AiError | RoutingUncertainError | NoEligibleProcedureError | DepthExceededError, RequirementsOf<Members[number]> | DecisionModel.DecisionModel | S['EncodingServices'], S>;

// @public (undocumented)
export type IdOf<C> = C extends Procedure<infer Id, any, any, any, any, any> ? Id : never;

// @public (undocumented)
export interface InvokeOptions<Input, Ids extends string, Fallback> {
    readonly onUncertain?: (input: Input, route: Extract<Route<Ids>, {
        _tag: 'Uncertain';
    }>) => Fallback;
    // (undocumented)
    readonly routing?: RouteOptions;
}

// @public
export const make: <const Id extends string, S extends Schema.Constraint, Out, Err, Req>(options: {
    readonly id: Id;
    readonly description: string;
    readonly examples?: ReadonlyArray<string>;
    readonly input: S;
    readonly eligible?: (input: S['Type']) => boolean;
    readonly run: (input: S['Type']) => Effect.Effect<Out, Err, Req>;
}) => Procedure<Id, S['Type'], Out, Err, Req, S>;

// @public
export const MaxDepth: Context.Reference<number>;

// @public (undocumented)
export class NoEligibleProcedureError extends Error {
    constructor(message: string);
    // (undocumented)
    readonly message: string;
    // (undocumented)
    readonly name = "NoEligibleProcedureError";
    // (undocumented)
    readonly _tag = "NoEligibleProcedureError";
}

// @public (undocumented)
export type OutputOf<C> = C extends Procedure<any, any, infer O, any, any, any> ? O : never;

// @public (undocumented)
export interface Procedure<Id extends string, Input, Output, Error, Requirements, InputSchema extends Schema.Constraint> {
    // (undocumented)
    readonly [ProcedureTypeId]: typeof ProcedureTypeId;
    readonly description: string;
    readonly eligible: (input: Input) => boolean;
    readonly examples: ReadonlyArray<string>;
    // (undocumented)
    readonly id: Id;
    // (undocumented)
    readonly input: InputSchema;
    // (undocumented)
    readonly run: (input: Input) => Effect.Effect<Output, Error, Requirements>;
}

// @public (undocumented)
export const ProcedureTypeId: unique symbol;

// @public (undocumented)
export interface Registry<Members extends ReadonlyArray<Any>, S extends Schema.Constraint, RouteInput extends Schema.Constraint = S> {
    // Warning: (ae-forgotten-export) The symbol "ClassifyDecision" needs to be exported by the entry point procedure.d.ts
    readonly decision: ClassifyDecision<RouteInput['Type'], IdOf<Members[number]>, RouteInput>;
    // (undocumented)
    readonly get: <Id extends IdOf<Members[number]>>(id: Id) => Extract<Members[number], {
        readonly id: Id;
    }>;
    // (undocumented)
    readonly ids: ReadonlyArray<IdOf<Members[number]>>;
    // (undocumented)
    readonly input: S;
    readonly invoke: <Fallback = never>(input: S['Type'], options?: InvokeOptions<S['Type'], IdOf<Members[number]>, Fallback>) => Effect.Effect<OutputOf<Members[number]> | EffectSuccess<Fallback>, ErrorOf<Members[number]> | EffectError<Fallback> | AiError.AiError | NoEligibleProcedureError | DepthExceededError | ([Fallback] extends [never] ? RoutingUncertainError : never), RequirementsOf<Members[number]> | EffectRequirements<Fallback> | DecisionModel.DecisionModel | RouteInput['EncodingServices']>;
    readonly invokeWithRoute: <Fallback = never>(input: S['Type'], options?: InvokeOptions<S['Type'], IdOf<Members[number]>, Fallback>) => Effect.Effect<{
        readonly route: Route<IdOf<Members[number]>>;
        readonly value: OutputOf<Members[number]> | EffectSuccess<Fallback>;
    }, ErrorOf<Members[number]> | EffectError<Fallback> | AiError.AiError | NoEligibleProcedureError | DepthExceededError | ([Fallback] extends [never] ? RoutingUncertainError : never), RequirementsOf<Members[number]> | EffectRequirements<Fallback> | DecisionModel.DecisionModel | RouteInput['EncodingServices']>;
    // (undocumented)
    readonly members: Members;
    readonly route: (input: S['Type'], options?: RouteOptions) => Effect.Effect<Route<IdOf<Members[number]>>, AiError.AiError, DecisionModel.DecisionModel | RouteInput['EncodingServices']>;
    readonly routeInput: RouteInput;
}

// @public
export const registry: <S extends Schema.Constraint, const Members extends ReadonlyArray<Procedure<string, S['Type'], any, any, any, S>>, RouteInput extends Schema.Constraint = S>(input: S, members: Members, options?: {
    readonly id?: string;
    readonly instructions?: string;
    readonly routeBy?: RouteBy<S, RouteInput>;
}) => Registry<Members, S, RouteInput>;

// @public (undocumented)
export type RequirementsOf<C> = C extends Procedure<any, any, any, any, infer R, any> ? R : never;

// @public (undocumented)
export type Route<Ids extends string> = {
    readonly _tag: 'Matched';
    readonly id: Ids;
    readonly probability: number;
    readonly margin: number;
    readonly by: 'model' | 'elimination';
    readonly ranked: ReadonlyArray<RouteCandidate<Ids>>;
} | {
    readonly _tag: 'Uncertain';
    readonly reason: string;
    readonly ranked: ReadonlyArray<RouteCandidate<Ids>>;
} | {
    readonly _tag: 'None';
    readonly reason: string;
};

// @public
export interface RouteBy<S extends Schema.Constraint, R extends Schema.Constraint> {
    // (undocumented)
    readonly schema: R;
    // (undocumented)
    readonly select: (input: S['Type']) => R['Type'];
}

// @public (undocumented)
export interface RouteCandidate<Ids extends string> {
    // (undocumented)
    readonly id: Ids;
    // (undocumented)
    readonly probability: number;
}

// @public (undocumented)
export interface RouteOptions {
    readonly minMargin?: number;
    readonly minProbability?: number;
}

// @public (undocumented)
export class RoutingUncertainError extends Error {
    constructor(route: Extract<Route<string>, {
        _tag: 'Uncertain';
    }>);
    // (undocumented)
    readonly name = "RoutingUncertainError";
    // (undocumented)
    readonly route: Extract<Route<string>, {
        _tag: 'Uncertain';
    }>;
    // (undocumented)
    readonly _tag = "RoutingUncertainError";
}

// @public
export const withMaxDepth: (limit: number) => <A, E, R>(effect: Effect.Effect<A, E, R>) => Effect.Effect<A, E, R>;

// (No @packageDocumentation comment for this package)

```
