# AGENTS.md — `@systemfsoftware/discern`

Root `AGENTS.md` governs; these are the facts no default gate reports.

| ID     | Rule                                                                                                                                                                                                                                                                                                                                                             | Gate                                                                                                                                           |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| **D2** | Replay, caching, recording, and budgets bind at the caller's edge through a `Discern.Model` layer; no policy, matcher, or registry provides on the caller's behalf. The one sanctioned `provideService` is `CurrentDepth` around a routed member's run (per-branch depth), in `procedure-depth.service.ts`, `region.service.ts`, and `invoke-procedure.cell.ts`. | `git grep -n -e 'Effect.provide(' -e 'Layer.provide' -- packages/discern/src` prints nothing                                                   |
| **D4** | Decisions reach the provider keyed by content address (`hash`/`decisionFingerprint` in `decision-model.blueprint.ts`), never by the caller's `id`. Changing either canonical form silently invalidates every recording and cache key already stored — the replay suites still pass because they record and replay in one run.                                    | `review` — wrong: a debug field added to the fingerprinted input; right: debug data carried beside the fingerprint, which stays byte-identical |
| **D5** | No defect hides in `src/`: no `Effect.die`, `orDie`, `getOrThrow`, `throw`, or cast. An impossible case is made unrepresentable; a cell's `CommandRejected` fails typed with its read context and the rejection as `cause` (`packages/effect-microsandbox/src/await-readiness.cell.ts`).                                                                         | `git grep -n -E 'Effect\.die\|orDie\|getOrThrow\|\bthrow\b\|as unknown\|@ts-expect-error' -- packages/discern/src` prints nothing              |
