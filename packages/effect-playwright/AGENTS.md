# AGENTS.md — `@systemfsoftware/effect-playwright`

Root `AGENTS.md` governs; these are the facts no default gate reports. Forked from [`Jobflow-io/effect-playwright`](https://github.com/Jobflow-io/effect-playwright) at `fd314a23748b727a28f963d12fd2a2b4018a4126` (0.8.0-2); upstream's MIT notice is retained in `NOTICE`.

| ID      | Rule                                                                                                                                                                                                       | Gate                                                                                      |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| **PW1** | Binary payloads — `screenshot`, `pdf`, `Request.postDataBuffer`, `Download.stream` — are typed `Uint8Array`, never `Buffer`; a `Buffer` reaches the surface only as a `Uint8Array` narrowed in one helper. | `git grep -n '\bBuffer\b' -- packages/effect-playwright/src` prints nothing               |
| **PW2** | No `src/` module imports a Node builtin (`node:*` or bare `fs`/`stream`/...); the only process boundary is Playwright itself.                                                                              | `git grep -n -E "from 'node:                                                              |
| **PW3** | The documented public API never sits under an `internal/` segment and no exported declaration carries `@internal`; an upstream-internal type that reaches a public signature is exported instead.          | `git grep -n -E '@internal\|/internal/' -- packages/effect-playwright/src` prints nothing |
| **PW4** | The `./test` entry's e2e lane holds at most four journeys, under `e2e/*.pw.ts`; a fifth journey belongs in a vitest integration feature, not the runner lane.                                              | `git grep -c 'test.effect(' -- packages/effect-playwright/e2e` prints at most `4`         |
