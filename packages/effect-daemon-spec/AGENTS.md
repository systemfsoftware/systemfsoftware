# AGENTS.md — `@systemfsoftware/effect-daemon-spec`

Root `AGENTS.md` governs; these are the facts no default gate reports.

| ID        | Rule                                                                                                                                                                                                                                                | Gate                                                                                                                           |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| **DS-A2** | `etc/effect-daemon-spec.api.md` reports the `Supervisor` namespace barrel and carries no `ae-forgotten-export` entry. A new one is fixed by exporting or inlining the type, or by a named base const for an effect class factory, never suppressed. | `review` — wrong: `api:update` committed with a new `ae-forgotten-export`; right: the type exported and the report regenerated |
| **DS-M1** | Never add `effect` to `bundledPackages` in `api-extractor.json`: it is a peer, and bundling freezes effect's types into the rollup.                                                                                                                 | `review` — wrong: `"bundledPackages": ["effect"]`; right: `bundledPackages` names no peer dependency                           |
