# AGENTS.md — `@systemfsoftware/effect-daemon-spec`

Root `AGENTS.md` governs; these are the facts no default gate reports.

| ID        | Rule                                                                                                                                                                                                                                                                                                                                                                                                     | Gate                                                                                                                                     |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| **DS-A2** | The 11 `ae-forgotten-export … _base` entries in `etc/effect-daemon-spec.api.md` are baseline — synthetic intermediates from effect's `Schema.Class`/`Context.Tag`/`TaggedError` factories. Never delete them; a new `*_base` warning needs a named base const at the source or explicit reviewer approval. Any other `ae-forgotten-export` is fixed by exporting or inlining the type, never suppressed. | `review` — wrong: `api:update` run after deleting a `_base` entry; right: a new `_base` warning answered with `const FooBase = S.Class…` |
| **DS-M1** | Never add `effect` to `bundledPackages` in `api-extractor.json`: it is a peer, and bundling freezes effect's types into the rollup.                                                                                                                                                                                                                                                                      | `review` — wrong: `"bundledPackages": ["effect"]`; right: `bundledPackages` names no peer dependency                                     |
