# AGENTS.md — `cell-imports/`

> Delta only. Shared rule-authoring conventions: `packages/oxlint-plugins/AGENTS.md`. Universal rules: root `AGENTS.md`.

This package will enforce one table, one rule: every cell-to-cell import edge in the taxonomy. The rule implementation is intentionally owned by a separate change.
