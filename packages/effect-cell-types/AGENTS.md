# AGENTS.md — `@systemfsoftware/effect-cell-types`

Root `AGENTS.md` governs; these are the facts no default gate reports.

| ID          | Rule                                                                                                                                                                                                                                                                                                 | Gate                                                                                                                                                   |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **CELL-T1** | No mutation enrollment — no `stryker.config.json`, no mutation script. The package holds types plus construction-time composition, so a mutation score here grades someone else's test suite.                                                                                                        | `git ls-files 'packages/effect-cell-types/**/stryker.config.*'` prints nothing                                                                         |
| **CELL-T2** | Every claim about the Cell surface is a type assertion in `test-types/*.tst.ts`; arrow application (`cell.run(input)`) is verified by composition tests in `tests/` running a real Cell. Never unit-test the composition glue alone, and never let a behavioural test stand in for a type assertion. | `review` — wrong: a vitest case asserting `typeof cell.run === 'function'`; right: `expect(cell.run).type.toBeCallableWith(input)` in a `.tst.ts` file |
| **CELL-T4** | Observation is a local closure, never a service on `R`.                                                                                                                                                                                                                                              | `git grep -n -E 'TraceRecorder\|recordSync' -- 'packages/effect-cell-types/*.ts'` prints nothing                                                       |
