/** Compatibility shim: the canonical composition lives in `@systemfsoftware/oxlint-preset`. */

import canonical from '@systemfsoftware/oxlint-preset'

export { defaultIgnores } from '@systemfsoftware/oxlint-preset'

export default canonical
