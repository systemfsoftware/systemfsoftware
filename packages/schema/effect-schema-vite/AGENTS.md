# AGENTS.md — `@systemfsoftware/effect-schema-vite`

Root `AGENTS.md` governs; this is the fact no default gate reports.

| ID          | Rule                                                                                                                                                                                                                                                                                                                                | Gate                                                                                                                                                            |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **VITE-V2** | Laws are injected by rewriting the consumer's real `src/schema-laws.test.ts` through the `transform` hook — never a virtual module: a virtual id has no path for vitest's `--related` walk, so every generated law is silently dropped. That basename is the only test filename `oxlint-plugin-test-discipline` whitelists by name. | `review` — wrong: `resolveId` returning `'\0virtual:schema-laws'`; right: `transform(code, id)` rewriting the module whose id ends in `src/schema-laws.test.ts` |
