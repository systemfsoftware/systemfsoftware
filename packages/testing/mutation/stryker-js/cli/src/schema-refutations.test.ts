// Generated: @systemfsoftware/effect-schema-refutation-vite rewrites this file,
// injecting one assertion that every obligation reachable from an exported Effect
// Schema in this package is refuted somewhere. The generated body carries real
// import edges to each schema file so vitest's related-file walk (and therefore
// `stryker --related`) reaches them. The plugin rewrites an existing path, so
// deleting this file silently disables the coverage assertion.
export {}
