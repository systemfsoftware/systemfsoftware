# AGENTS.md — `packages/atom/`

Root `AGENTS.md` governs; these are the facts no default gate reports.

| ID      | Rule                                                                                                                                                                                                                                                                                                                                                             | Gate                                                                                                                                                                |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **AT3** | Import type-only members directly (`import { getResult, type Registry } from './Registry'`), never through a namespace import: oxlint's `import/namespace` resolves the runtime `.mjs`, where type-only exports do not exist. Fix the import; never silence the rule.                                                                                            | `pnpm --filter @systemfsoftware/effect-atom lint`                                                                                                                   |
| **AT4** | Mutation covers only `effect-atom/src/atom-node.ts`, through the node-only `vitest.node.config.ts` (CI's Mutation job installs no browser). Adding `registry-engine.ts` pushed the CI job past its 30-minute limit with no report; `src/internal/node-lifetime.ts` observes process lifetime, so its mutants prove nothing. `effect-atom-react` is not enrolled. | `git ls-files 'packages/atom/effect-atom-react/stryker.config.*'` prints nothing and `grep -c node-lifetime packages/atom/effect-atom/stryker.config.ts` prints `0` |
| **AT5** | TypeId and hydration strings are wire format: `~effect/reactivity/Atom`, `~effect/reactivity/AtomRef`, `~effect-atom/atom/Registry`, `~@effect/atom-react/ScopedAtom`, `~effect/reactivity/DehydratedAtom`, and each serializable atom's key. No package or module rename changes them.                                                                          | `review` — wrong: a rename makes the TypeId `Symbol.for('~@systemfsoftware/effect-atom/Atom')`; right: the literal stays `'~effect/reactivity/Atom'`                |
| **AT6** | Browser tests run in each package's `browser` Vitest project (playwright, headless chromium) — never jsdom or jest-dom.                                                                                                                                                                                                                                          | `git grep -l -e jsdom -e jest-dom -- 'packages/atom/*/package.json'` prints nothing                                                                                 |

Neither package models an outside interaction as a Cell, so `sandwich-phase-order`, `pure-decision-workflows`, `four-channel-contracts`, `pipeline-composition`, and `ports-separate-from-layers` do not apply until a module reads, decides, and writes against the outside world. `staged-lawful-builders` does apply: an atom is a blueprint (`effect-atom/src/atom.blueprint.ts`).
