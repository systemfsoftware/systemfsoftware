## API Report File for "@systemfsoftware/effect-gherkin-spec"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Context } from 'effect';
import * as Effect$1 from 'effect/Effect';
import { Effect } from 'effect';
import * as EffectVitest from '@effect/vitest';
import * as Layer$1 from 'effect/Layer';
import { Layer } from 'effect';
import { Schema } from 'effect';
import * as Scope from 'effect/Scope';
import { TestOptions } from 'vitest';
import * as TestServices from 'effect/TestServices';
import { Vitest } from '@effect/vitest';

// Warning: (ae-forgotten-export) The symbol "NoInfer$1" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export const And: (text: StepText) => <A extends object, E2 = never, R2 = never>(f: (a: NoInfer$1<A>) => Effect.Effect<unknown, E2, R2> | void) => <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A, E1 | StepError, R1 | R2>;

// @public (undocumented)
export const But: (text: StepText) => <A extends object, E2 = never, R2 = never>(f: (a: NoInfer$1<A>) => Effect.Effect<unknown, E2, R2> | void) => <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A, E1 | StepError, R1 | R2>;

// @public (undocumented)
export type EffectVitestDeps = Pick<typeof EffectVitest, 'layer'> & {
    readonly it: Vitest.Methods;
};

// Warning: (ae-forgotten-export) The symbol "EmptyScopeMap$1" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type FeatureBody<RShared = never, RFresh = never, RFreshReq = never, S extends ScopeMap = EmptyScopeMap$1> = (ctx: {
    readonly scenario: ScenarioFn<RShared, RFresh, RFreshReq>;
    readonly background: (pipeline: Effect$1.Effect<unknown, StepError, RShared | RFresh | RFreshReq>) => void;
    readonly scenarioOutline: OutlineFn<RShared, RFresh, RFreshReq>;
    readonly scope: GherkinEffect<ScopeServices<S>, never, ScopeIdentifiers<S>>;
    readonly Do: Effect$1.Effect<object, never, never>;
}) => void;

// Warning: (ae-forgotten-export) The symbol "EmptyScopeMap" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type FeatureBuilder<S extends ScopeMap = EmptyScopeMap> = {
    readonly liveClock: () => FeatureBuilder<S>;
    body: (body: FeatureBody<never, never, never, S>) => void;
    withLayer: <RShared>(layer: Layer$1.Layer<RShared>, opts?: FeatureLayerOptions) => FeatureBuilderWithLayer<RShared, S>;
    withScenarioLayer: <RFresh, RFreshReq extends TestServices.TestServices | Scope.Scope = never>(layer: Layer$1.Layer<RFresh, never, RFreshReq>) => FeatureBuilderWithScenarioLayer<RFresh, RFreshReq, S>;
    withScope: <S2 extends ScopeMap>(map: S2) => FeatureBuilder<S2>;
};

// @public (undocumented)
export type FeatureBuilderBoth<RShared, RFresh, RFreshReq extends RShared | TestServices.TestServices | Scope.Scope, S extends ScopeMap = EmptyScopeMap> = {
    readonly liveClock: () => FeatureBuilderBoth<RShared, RFresh, RFreshReq, S>;
    body: (body: FeatureBody<RShared, RFresh, RFreshReq, S>) => void;
    withScope: <S2 extends ScopeMap>(map: S2) => FeatureBuilderBoth<RShared, RFresh, RFreshReq, S2>;
};

// @public (undocumented)
export type FeatureBuilderWithLayer<RShared, S extends ScopeMap = EmptyScopeMap> = {
    readonly liveClock: () => FeatureBuilderWithLayer<RShared, S>;
    body: (body: FeatureBody<RShared, never, never, S>) => void;
    withScenarioLayer: <RFresh, RFreshReq extends RShared | TestServices.TestServices | Scope.Scope = never>(layer: Layer$1.Layer<RFresh, never, RFreshReq>) => FeatureBuilderBoth<RShared, RFresh, RFreshReq, S>;
    withScope: <S2 extends ScopeMap>(map: S2) => FeatureBuilderWithLayer<RShared, S2>;
};

// @public (undocumented)
export type FeatureBuilderWithScenarioLayer<RFresh, RFreshReq extends TestServices.TestServices | Scope.Scope, S extends ScopeMap = EmptyScopeMap> = {
    readonly liveClock: () => FeatureBuilderWithScenarioLayer<RFresh, RFreshReq, S>;
    body: (body: FeatureBody<never, RFresh, RFreshReq, S>) => void;
    withLayer: <RShared>(layer: Layer$1.Layer<RShared>, opts?: FeatureLayerOptions) => FeatureBuilderBoth<RShared, RFresh, RFreshReq, S>;
    withScope: <S2 extends ScopeMap>(map: S2) => FeatureBuilderWithScenarioLayer<RFresh, RFreshReq, S2>;
};

// Warning: (ae-forgotten-export) The symbol "FeatureStarter" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type FeatureFn = FeatureStarter & {
    readonly skip: FeatureStarter;
    readonly only: FeatureStarter;
};

// @public (undocumented)
export type FeatureLayerOptions = {
    readonly excludeTestServices?: boolean;
};

// @public (undocumented)
export type FeatureSuiteOptions = Pick<TestOptions, 'tags'> & Partial<TestOptions>;

// @public (undocumented)
export const Gherkin: {
    readonly Do: Effect.Effect<GherkinScope<Record<string, never>>, never, never>;
    readonly startWith: <A extends object>(bindings: A) => GherkinEffect<A, never, never>;
    readonly scope: typeof makeScope;
    readonly Given: (text: StepText) => {
        <N extends string, A extends object, B, E2, R2>(name: N, f: (a: NoInfer$1<A>) => Effect.Effect<B, E2, R2>): <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A & Record<N, B>, E1 | StepError, R1 | R2>;
        <A extends object, E2 = never, R2 = never>(f: (a: NoInfer$1<A>) => Effect.Effect<unknown, E2, R2> | void): <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A, E1 | StepError, R1 | R2>;
    };
    readonly When: (text: StepText) => {
        <N extends string, A extends object, B, E2, R2>(name: N, f: (a: NoInfer$1<A>) => Effect.Effect<B, E2, R2>): <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A & Record<N, B>, E1 | StepError, R1 | R2>;
        <A extends object, E2 = never, R2 = never>(f: (a: NoInfer$1<A>) => Effect.Effect<unknown, E2, R2> | void): <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A, E1 | StepError, R1 | R2>;
    };
    readonly Then: (text: StepText) => <A extends object, E2 = never, R2 = never>(f: (a: NoInfer$1<A>) => Effect.Effect<unknown, E2, R2> | void) => <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A, E1 | StepError, R1 | R2>;
    readonly And: (text: StepText) => <A extends object, E2 = never, R2 = never>(f: (a: NoInfer$1<A>) => Effect.Effect<unknown, E2, R2> | void) => <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A, E1 | StepError, R1 | R2>;
    readonly But: (text: StepText) => <A extends object, E2 = never, R2 = never>(f: (a: NoInfer$1<A>) => Effect.Effect<unknown, E2, R2> | void) => <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A, E1 | StepError, R1 | R2>;
};

// @public (undocumented)
export type GherkinEffect<A extends object, E, R> = Effect.Effect<GherkinScope<A>, E, R>;

// @public (undocumented)
export type GherkinScope<A extends object> = A & {
    readonly [GherkinScopeTypeId]: typeof GherkinScopeTypeId;
};

// @public (undocumented)
export const Given: (text: StepText) => {
    <N extends string, A extends object, B, E2, R2>(name: N, f: (a: NoInfer$1<A>) => Effect.Effect<B, E2, R2>): <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A & Record<N, B>, E1 | StepError, R1 | R2>;
    <A extends object, E2 = never, R2 = never>(f: (a: NoInfer$1<A>) => Effect.Effect<unknown, E2, R2> | void): <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A, E1 | StepError, R1 | R2>;
};

// @public (undocumented)
export const makeFeature: (deps: EffectVitestDeps) => FeatureFn;

// Warning: (ae-forgotten-export) The symbol "OutlineCallable" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type OutlineFn<RShared = never, RFresh = never, RFreshReq = never> = OutlineCallable<RShared, RFresh, RFreshReq> & {
    readonly skip: OutlineCallable<RShared, RFresh, RFreshReq>;
    readonly only: OutlineCallable<RShared, RFresh, RFreshReq>;
};

// Warning: (ae-forgotten-export) The symbol "NoInfer_2" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export const pairwiseFor: <Identifier, Service, RA = never, RB = never>(matrix: PairwiseMatrix<Identifier, RA, RB>, service: Context.Tag<Identifier, Service>) => (text: StepText) => <N extends string, A extends object, Out, E>(name: N, f: (scope: NoInfer_2<A>) => (svc: Service) => Effect.Effect<Out, E, never>) => <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A & Record<N, PairwiseResult<Out>>, E1 | StepError, R1 | RA | RB>;

// @public (undocumented)
export interface PairwiseMatrix<Identifier = unknown, RA = never, RB = never> {
    // (undocumented)
    readonly a: {
        readonly name: string;
        readonly layer: Layer.Layer<Identifier, never, RA>;
    };
    // (undocumented)
    readonly b: {
        readonly name: string;
        readonly layer: Layer.Layer<Identifier, never, RB>;
    };
}

// @public (undocumented)
export type PairwiseResult<A> = {
    readonly a: A;
    readonly b: A;
    readonly aLabel: string;
    readonly bLabel: string;
};

// @public (undocumented)
export type ScenarioBody<R = never> = Effect$1.Effect<unknown, StepError, R>;

// Warning: (ae-forgotten-export) The symbol "ScenarioCallable" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type ScenarioFn<RShared = never, RFresh = never, RFreshReq = never> = ScenarioCallable<RShared, RFresh, RFreshReq> & {
    readonly skip: ScenarioCallable<RShared, RFresh, RFreshReq>;
    readonly only: ScenarioCallable<RShared, RFresh, RFreshReq>;
};

// @public (undocumented)
export type ScenarioOptions<RScenario = never, RExtra = never> = {
    readonly scenarioLayer?: Layer$1.Layer<RScenario>;
    readonly layer?: Layer$1.Layer<RExtra>;
};

// @public (undocumented)
export type ScopeIdentifiers<S extends ScopeMap> = { [K in keyof S]: S[K] extends Effect.Effect<infer _A, never, infer R> ? R : never; }[keyof S];

// @public (undocumented)
export type ScopeMap = Readonly<Record<string, Effect.Effect<unknown, never, unknown>>>;

// @public (undocumented)
export type ScopeServices<S extends ScopeMap> = { readonly [K in keyof S]: S[K] extends Effect.Effect<infer A, never, infer _R> ? A : never; };

// Warning: (ae-forgotten-export) The symbol "StepError_base" needs to be exported by the entry point index.d.ts
//
// @public
export class StepError extends StepError_base {}

// @public (undocumented)
export type StepText<A extends object = object> = string | ((scope: A) => string);

// @public (undocumented)
export const Then: (text: StepText) => <A extends object, E2 = never, R2 = never>(f: (a: NoInfer$1<A>) => Effect.Effect<unknown, E2, R2> | void) => <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A, E1 | StepError, R1 | R2>;

// @public (undocumented)
export const When: (text: StepText) => {
    <N extends string, A extends object, B, E2, R2>(name: N, f: (a: NoInfer$1<A>) => Effect.Effect<B, E2, R2>): <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A & Record<N, B>, E1 | StepError, R1 | R2>;
    <A extends object, E2 = never, R2 = never>(f: (a: NoInfer$1<A>) => Effect.Effect<unknown, E2, R2> | void): <E1, R1>(self: GherkinEffect<A, E1, R1>) => GherkinEffect<A, E1 | StepError, R1 | R2>;
};

// Warnings were encountered during analysis:
//
// dist/index.d.ts:38:3 - (ae-forgotten-export) The symbol "makeScope" needs to be exported by the entry point index.d.ts

// (No @packageDocumentation comment for this package)

```
